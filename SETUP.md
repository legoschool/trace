# 🚚 내 기록장 만들기 · 처음부터 끝까지

> **만든이 · 레고학교 미스터리** · 오픈소스 · 적정지식의 범위 안에서
>
> **자기 사본을 만들어 쓰는 문서입니다.**
> 기록은 각자의 구글 드라이브에 쌓이므로, 사본을 만들면 남의 손이 닿지 않는 내 것이 됩니다.
>
> 예전에는 **한 프로젝트당 평생 100명** 한도가 있었지만, 권한을 좁혀 **그 한도를 없앴습니다.**
> 대신 밖에서 만들어 넣은 파일은 **「드라이브에서 파일 고르기」로 한 번 골라 주어야** 보입니다.

---

## 시작하기 전에

**필요한 것은 두 가지뿐입니다.** 구글 계정 하나, 깃허브 계정 하나.
프로그램을 깔 필요도, 돈을 낼 일도 없습니다.

**처음이면 35분쯤** 걸립니다. 아래 사진을 **위에서 아래로 그대로 따라 하면** 됩니다.
**붉은 네모가 지금 눌러야 할 곳**입니다.

이 앱은 **서버가 없습니다.** 각자의 브라우저가 **자기 구글 계정**으로 **자기 드라이브**에
직접 저장합니다. 그래서 나눠 주는 사람은 서버 비용도, 저장 공간도, 남의 데이터를
관리할 책임도 지지 않습니다. **남의 기록을 볼 수도 없습니다.**

전체 흐름은 이렇습니다.

| 부 | 하는 일 | 걸리는 시간 |
|---|---|---|
| 1부 | 깃허브에 내 저장소 만들기 | 5분 |
| 2부 | 사이트 켜기 | 5분 |
| 3부 | 구글 클라우드 설정 | 20분 |
| 4부 | 코드에 내 열쇠 심기 | 2분 |
| 5부 | 첫 로그인과 폴더 연결 | 3분 |

> 📌 **구글 클라우드에서 챙길 것이 넷입니다.** 3부에서 차례로 나옵니다.
> ① Drive API ② **Picker API** ③ 클라이언트 ID ④ **API 키**
> ②와 ④는 폴더·파일 고르기 창에 쓰입니다. 빠뜨리면 첫 연결부터 막히니
> 3부는 건너뛰지 말고 순서대로 따라 하세요.

---

# 1부 · 깃허브에 내 저장소 만들기

## 1. 원본 저장소에서 `Use this template` 를 누릅니다

원본 저장소 화면 오른쪽 위입니다.

![Use this template 버튼](docs/images/setup-01.png)

## 2. 메뉴에서 `Create a new repository` 를 고릅니다

![Create a new repository](docs/images/setup-02.png)

## 3. 저장소 이름을 적습니다

**영문 소문자와 하이픈(-)** 으로 지으세요. 한글로 지으면 주소가 깨집니다.
여기서는 `my-googlenote` 로 했습니다. 초록 글씨로 **「is available」** 이 뜨면 쓸 수 있는 이름입니다.

이 이름이 그대로 **내 사이트 주소**가 됩니다 → `내계정.github.io/my-googlenote/`

![저장소 이름 넣기](docs/images/setup-03.png)

## 4. `Public` 인지 확인하고 `Create repository` 를 누릅니다

**반드시 `Public` 이어야 합니다.** `Private` 이면 사이트가 열리지 않습니다.
(코드만 공개되는 것이고, **여러분의 기록은 공개되지 않습니다.** 기록은 여러분 드라이브에 있습니다.)

![Public 확인하고 Create repository](docs/images/setup-04.png)

## 5. 내 저장소가 만들어졌습니다

![새로 만들어진 저장소](docs/images/setup-05.png)

---

# 2부 · 사이트 켜기

지금은 코드만 있고 **주소가 없는 상태**입니다. 이제 켭니다.

## 6. 위쪽 `Settings` 탭

![Settings 탭](docs/images/setup-06.png)

## 7. 왼쪽 메뉴를 내려서 `Pages`

![왼쪽 메뉴의 Pages](docs/images/setup-07.png)

## 8. `Source` 아래 드롭다운을 누릅니다

지금은 `Deploy from a branch` 라고 되어 있습니다.

![Source 드롭다운](docs/images/setup-08.png)

## 9. `GitHub Actions` 를 고릅니다

![GitHub Actions 고르기](docs/images/setup-09.png)

## 10. 이렇게 바뀌면 됩니다

따로 저장 버튼은 없습니다. 고르면 바로 적용됩니다.

![Source 가 GitHub Actions 로 바뀐 상태](docs/images/setup-10.png)

## 11. ⚠️ `Actions` 탭에 빨간 ✗ 가 있습니다. 정상입니다

저장소를 만들 때 배포가 **한 번 자동으로 돌았는데, 그때는 아직 Pages 를 안 켠 상태**라
실패한 것입니다. **다시 돌리면 됩니다.**

`Actions` 탭 → 실패한 줄을 누르고 → 오른쪽 위 `Re-run jobs`

![실패한 실행과 Re-run jobs](docs/images/setup-11.png)

## 12. `Re-run all jobs` 를 고릅니다

![Re-run all jobs](docs/images/setup-12.png)

작은 확인 창이 뜨면 초록색 **`Re-run jobs`** 를 한 번 더 누릅니다.

## 13. 1~2분 뒤 초록 ✓ 로 바뀝니다

![초록 체크로 바뀐 실행 목록](docs/images/setup-13.png)

## 14. `Settings → Pages` 로 돌아가면 주소가 나와 있습니다

**「Your site is live at …」** 옆의 주소가 **내 기록장 주소**입니다.
(아래 사진에서는 가려 두었습니다.) **이 주소를 메모해 두세요.** 뒤에서 씁니다.

![Your site is live at](docs/images/setup-14.png)

> 여기까지 하면 사이트는 열립니다. 다만 아직 **로그인은 안 됩니다.**
> 로그인 열쇠(클라이언트 ID)를 3부에서 만듭니다.

---

# 3부 · 구글 클라우드 설정

여기가 제일 길지만, **한 번만 하면 끝**입니다.
[console.cloud.google.com](https://console.cloud.google.com) 에 내 구글 계정으로 들어갑니다.

## 15. 맨 위 왼쪽 **프로젝트 이름 칸**을 누릅니다

![상단 프로젝트 칩](docs/images/setup-15.png)

## 16. 창 오른쪽 위 `새 프로젝트`

![새 프로젝트](docs/images/setup-16.png)

## 17. 이름을 넣고 `만들기`

프로젝트 이름은 아무거나 좋습니다. `my-googlenote` 처럼 알아보기 쉬운 걸로 하세요.
만드는 데 10초쯤 걸립니다.

> ⚠️ **여기서 가장 많이 틀립니다.**
> 만들고 나면 **맨 위 칸이 새 프로젝트 이름으로 바뀌었는지 꼭 확인하세요.**
> 예전 프로젝트가 선택된 채로 아래 작업을 하면 전부 헛일이 됩니다.

> 💡 **「프로젝트 한도에 도달했습니다」가 뜨면** 이미 프로젝트가 너무 많은 것입니다.
> 안 쓰는 프로젝트를 지워야 하는데, **지워도 30일이 지나야 자리가 빕니다.**
> 급하면 다른 구글 계정으로 하세요.

## 18. `Google Drive API` 를 켭니다

위쪽 검색칸에 **`Google Drive API`** 를 치고 들어가서 파란 **`사용`** 버튼을 누릅니다.

![Google Drive API 페이지](docs/images/setup-26.png)

> 위 사진은 **이미 켜 둔 화면**이라 `관리` 로 보입니다.
> 처음 만든 프로젝트에서는 같은 자리에 **파란 `사용` 버튼**이 있습니다. 그걸 누르면 됩니다.

## 19. `Google Picker API` 도 같은 방법으로 켭니다

검색칸에 **`Google Picker API`** 를 치고 들어가 **`사용`** 을 누릅니다.

> ⚠️ **이걸 빠뜨리면 「폴더 고르기」 창이 안 뜹니다.**
> 권한을 좁힌 뒤로 폴더와 파일을 고르는 일은 전부 이 창이 맡습니다.

## 20. `브랜딩` · 로그인 화면에 보일 이름

왼쪽 메뉴 `Google 인증 플랫폼` → `브랜딩` 입니다.

- **앱 이름**: 쓰는 사람에게 보일 이름. `구글 기록장` 처럼 적으면 됩니다
- **사용자 지원 이메일**: 내 구글 주소를 고릅니다

![브랜딩 · 앱 이름과 지원 이메일](docs/images/setup-17.png)

## 21. `대상` · 누가 쓸 수 있는지

- **사용자 유형**은 **`외부`**
- **게시 상태**는 **`프로덕션`**

![대상 · 게시 상태와 사용자 유형](docs/images/setup-18.png)

> ⚠️ **`테스트로 돌아가기` 를 누르지 마세요.**
> 테스트로 되돌리면 **미리 등록한 사람만** 쓸 수 있게 바뀌어 오히려 불편해집니다.

## 22. `데이터 액세스` · 권한 3개 ⚠️ 여기가 제일 많이 막힙니다

왼쪽 메뉴 `데이터 액세스` → **`범위 추가 또는 삭제`**

![범위 추가 또는 삭제](docs/images/setup-19.png)

## 23. 오른쪽에 열린 표에서 두 개를 체크합니다

- `.../auth/userinfo.email`
- `openid`

![email 과 openid 체크](docs/images/setup-22.png)

## 24. 같은 창을 **맨 아래까지 내려서** 나머지 하나를 직접 넣습니다

`직접 범위 추가` 칸에 아래 주소를 그대로 붙여넣고 **`테이블에 추가`** 를 누릅니다.

```
https://www.googleapis.com/auth/drive.file
```

> ⚠️ **뒤에 `.file` 이 꼭 붙어야 합니다.**
> `.file` 이 없는 `.../auth/drive` 는 «드라이브 전체» 권한이라, 심사 전까지 **100명 한도**에 걸립니다.
> `.file` 이 붙은 쪽은 심사도 한도도 없습니다.

![직접 범위 추가와 테이블에 추가](docs/images/setup-23.png)

## 25. 창 맨 아래 `업데이트`

![업데이트 버튼](docs/images/setup-24.png)

## 26. ⚠️ 창이 닫히면 **본 화면에서 `저장`을 한 번 더** 누릅니다

**저장을 두 번 눌러야 합니다.** 창 안의 `업데이트` 만 누르고 나가면 **저장되지 않습니다.**
여기서 막히는 분이 가장 많습니다.

## 27. 저장이 끝나면 표에 `.../auth/drive.file` 이 들어가 있습니다

`drive.file` 은 넓은 권한이 아니라서 **`제한된 범위` 가 아닌 다른 칸**에 들어갑니다.
어느 칸이든 목록에 `.../auth/drive.file` 이 보이면 성공입니다.

![범위가 등록된 결과](docs/images/setup-25.png)

## 28. `클라이언트` → `클라이언트 만들기`

![클라이언트 만들기](docs/images/setup-20.png)

## 29. `애플리케이션 유형` 을 누릅니다

![애플리케이션 유형](docs/images/setup-21.png)

## 30. 목록에서 `웹 애플리케이션`

![웹 애플리케이션 고르기](docs/images/setup-27.png)

## 31. 이름을 적습니다

여기 이름은 나만 보는 것이라 아무거나 좋습니다.

![유형과 이름을 채운 모습](docs/images/setup-28.png)

## 32. `승인된 JavaScript 원본` 에 **내 주소**를 넣습니다

`+ URI 추가` 를 누르고, **14번에서 메모해 둔 주소**를 넣되 · 

> ⚠️ **주소 뒷부분(저장소 이름과 슬래시)은 빼고 넣습니다.**
>
> - 내 사이트 주소가 `https://내계정.github.io/my-googlenote/` 라면
> - 여기에 넣을 것은 **`https://내계정.github.io`**
>
> 뒤에 `/` 나 경로가 붙으면 **로그인이 안 됩니다.**

![승인된 JavaScript 원본](docs/images/setup-29.png)

## 33. 맨 아래 `만들기`

![만들기 버튼](docs/images/setup-30.png)

## 34. **클라이언트 ID** 를 복사합니다

만들고 나면 창이 하나 뜹니다. **`클라이언트 ID`** 를 복사해 두세요.
창을 닫았어도 괜찮습니다. `클라이언트` 목록에서 복사 아이콘을 누르면 됩니다.

![클라이언트 목록에서 ID 복사](docs/images/setup-31.png)

> ⚠️ **「클라이언트 보안 비밀번호」는 쓰지 않습니다.** 어디에도 붙여넣지 마세요.
> **클라이언트 ID** 는 공개용 이름표라 코드에 넣어도 안전합니다. 둘을 헷갈리지 마세요.

## 35. **API 키**를 하나 더 만듭니다 ⚠️ 메뉴가 다릅니다

폴더·파일 고르기 창은 클라이언트 ID 말고 **API 키**를 따로 요구합니다.

> ⚠️ **API 키는 「Google 인증 플랫폼」 왼쪽 메뉴에 없습니다.**
> 거기를 나와 **`API 및 서비스` → `사용자 인증 정보`** 로 갑니다.
>
> ```
> https://console.cloud.google.com/apis/credentials
> ```

위쪽 **`+ 사용자 인증 정보 만들기`** → **`API 키`**. 창이 뜨면 이렇게 채웁니다.

| 칸 | 할 일 |
|---|---|
| 이름 | 아무거나 |
| **`API 제한사항 선택`** ＊ | **`Google Picker API`** 체크 (Drive API 도 같이 체크해도 됩니다) |
| `서비스 계정을 통해 API 호출 인증` | **체크하지 않음** |
| `애플리케이션 제한사항` | `웹사이트` (아래 참고) |

`AIza...` 로 시작하는 값이 나옵니다. **복사해 두세요.**

> ⚠️ 드롭다운에는 **켜 둔 API만** 나옵니다. `Google Picker API` 가 안 보이면
> 19번을 안 하신 것이니 먼저 켜고 오세요.

> 💡 API 키도 클라이언트 ID처럼 **공개용**이라 코드에 넣어도 됩니다.
> 다만 남이 퍼다 쓰면 **내 프로젝트의 할당량**을 갉아먹으니, `애플리케이션 제한사항` 을
> **`웹사이트`** 로 두고 `+ Add` 로 아래를 넣어 두세요. **끝의 `*` 를 빠뜨리면 안 됩니다.**
>
> ```
> https://내계정.github.io/*
> ```
>
> 반영에 **최대 5분**이 걸립니다. 그 사이에 고르기 창이 잠깐 안 떠도 정상입니다.

---

# 4부 · 코드에 내 열쇠 심기

## 36. 내 저장소의 `index.html` 을 엽니다

`Code` 탭 → `index.html` → **`682행`** 근처에 `OWNER_CLIENT_ID` 가 있습니다.
바로 위에 **「0. 소유자 설정 · 여기만 바꾸면 됩니다」** 라는 안내가 붙어 있으니
줄 번호가 다르면 그 글자로 찾으세요.

오른쪽 위 **연필 모양**을 누르면 고칠 수 있습니다.

![index.html 의 OWNER_CLIENT_ID 와 연필](docs/images/setup-32.png)

## 37. 따옴표 **안쪽만** 내 값으로 바꾸고 `Commit changes…`

**두 줄을 바꿉니다.** 바로 붙어 있습니다.

```js
var OWNER_CLIENT_ID = "여기에_내_클라이언트_ID";
var OWNER_API_KEY   = "여기에_내_API_키";
```

**따옴표와 세미콜론은 지우지 마세요.** 안쪽 글자만 바꿉니다.

> ⚠️ **API 키를 비워 두면** 로그인·저장은 되지만 **「폴더 고르기」 창이 안 뜹니다.**
> 폴더를 못 고르면 첫 연결부터 막히니 꼭 함께 넣으세요.

![Commit changes 버튼](docs/images/setup-33.png)

누르면 작은 창이 뜹니다. 그대로 초록 **`Commit changes`** 를 한 번 더 누르면 됩니다.
**1~2분 뒤 자동으로 다시 배포**됩니다.

---

# 5부 · 첫 로그인과 폴더 연결

## 38. 내 주소로 들어가서 `구글 로그인`

![앱 첫 화면과 구글 로그인](docs/images/setup-34.png)

## 39. 계정을 고릅니다

![계정을 선택하세요](docs/images/setup-35.png)

## 40. 「Google에서 확인하지 않은 앱」 화면이 나옵니다. 정상입니다

빨간 경고 삼각형이 떠서 놀라기 쉬운데, **개인이나 학교가 만든 앱은 모두 이 화면이 나옵니다.**
왼쪽 아래 작은 **`고급`** 을 누르세요.

![Google에서 확인하지 않은 앱](docs/images/setup-37.png)

## 41. 아래에 생긴 `(내 주소)(으)로 이동` 을 누릅니다

`고급` 을 누르면 아래쪽에 링크가 하나 생깁니다. 뒤에 **(안전하지 않음)** 이라고 붙어 있어도
**내가 만든 내 앱**이니 그대로 누르시면 됩니다.

![고급을 편 뒤 나오는 이동 링크](docs/images/setup-38.png)

## 42. 계정을 확인하고 `계속`

![계정 확인](docs/images/setup-39.png)

## 43. 권한을 확인하고 `계속`

드라이브 권한을 확인하는 화면입니다. 맨 아래 **`계속`** 을 누르면 로그인이 끝납니다.

> 💡 **「앱에서 사용하는 특정 Google Drive 파일에 한해」** 라고 나오면 맞게 하신 것입니다.
> 「Google Drive의 **모든** 파일」 이라고 나오면 24번에서 `.file` 을 빠뜨린 것이니 돌아가 고치세요.
> 체크 상자가 나오면 모두 켠 채로 `계속` 하시면 됩니다.

![권한 확인](docs/images/setup-40.png)

## 44. 앱의 `⚙️ 설정` → `저장 위치` 로 갑니다

로그인하면 설정 창이 저절로 열립니다. 안 열리면 오른쪽 위 `⚙️ 설정` 을 누르세요.

![설정의 폴더 칸](docs/images/setup-36.png)

## 45. 폴더를 정합니다 · 처음이라면 `＋ 새 폴더 만들기`

- **처음이면** **`＋ 새 폴더 만들기`** 가 가장 빠릅니다.
  이름을 적으면 내 드라이브 맨 위에 만들고 **바로 연결**합니다.
- **이미 쓰던 폴더가 있으면** **`📂 폴더 고르기`** 를 누릅니다.
  구글 고르기 창이 뜨면 **폴더를 한 번만 눌러 파랗게 고른 다음** 왼쪽 아래 `Select`.

> ⚠️ **두 번 누르면 폴더 «안» 으로 들어가 버립니다.** 그러면 `Select` 가 흐려집니다.
> 위쪽 경로에서 한 단계 나와 다시 한 번만 누르세요.

> 💡 폴더가 많으면 고르기 창 위쪽 **검색칸**에 이름을 쳐서 찾으세요.
> 기록장이 만든 날짜 폴더들이 함께 보여 헷갈리기 쉽습니다.

> 💡 **주소를 붙여넣는 칸**도 남아 있지만 대개 막힙니다.
> 권한을 좁힌 뒤로는 **「고르는 행위」 자체가 그 폴더에 권한을 주기** 때문입니다.

초록 띠로 연결됐다고 나오면 **끝입니다.**
이제부터 쓰는 기록은 자동으로 그 폴더에 올라갑니다.

## 46. 밖에서 만들어 온 파일이 있다면

드라이브에 손으로 넣어 둔 `.md` 나 사진은 **그냥은 안 잡힙니다.**
`⚙️ 설정` → `📥 가져오기` → **`📂 드라이브에서 파일 고르기`** 로 한 번 골라 주세요.
**한 번 고른 파일은 그 뒤로 저절로 잡힙니다.** 매번 고를 필요는 없습니다.

---

# 6부 · 내 컴퓨터에 앱으로 설치하기 (선택)

**안 해도 됩니다.** 다만 해 두면 **바탕화면 아이콘으로 바로 열리고**, 주소창 없는
자기 창으로 떠서 훨씬 앱처럼 쓸 수 있습니다.

## 47. 머리말의 `⬇ 앱으로 설치` 를 누릅니다

내 기록장을 크롬으로 연 다음, 위쪽 단추 줄에 있는 **`⬇ 앱으로 설치`** 를 누르면 됩니다.
(크롬 주소창 오른쪽 끝의 작은 **설치 아이콘**을 눌러도 같습니다.)

확인 창이 뜨면 **`설치`** 를 누르세요.

> 💡 **버튼이 안 보인다면** 이미 설치했거나, 크롬이 아직 준비되지 않은 것입니다.
> 페이지를 한 번 새로 고치고 잠시 뒤에 다시 보세요.
> 사파리·파이어폭스에서는 이 버튼이 나오지 않습니다. **크롬이나 엣지**를 쓰세요.

## 48. 이제 바탕화면·시작 메뉴에서 열립니다

설치하면 **「기록장」** 이라는 이름으로 아이콘이 생깁니다.
누르면 브라우저 탭이 아니라 **독립된 창**으로 열립니다. 구글 로그인도 그 창에서 그대로 됩니다.

- **지우고 싶으면** 앱 창 오른쪽 위 `⋮` → `기록장 제거`
- **휴대폰에서도** 크롬으로 열고 `⋮` → `홈 화면에 추가` 를 하면 같은 모양으로 쓸 수 있습니다

> ⚠️ **설치해도 인터넷은 있어야 합니다.** 기록을 구글 드라이브에 저장하기 때문입니다.
> 연결이 끊긴 채로 열면 안내 문구가 뜹니다.
> 코드를 고쳐 다시 배포하면 **설치한 앱에도 그대로 반영**됩니다. 다시 설치할 필요 없습니다.


---

# 막혔을 때

| 증상 | 이유와 해결 |
|---|---|
| 사이트 주소가 안 열림 | 2부의 `Pages` 를 안 켰거나, 저장소가 `Private` 입니다 |
| `Actions` 에 빨간 ✗ | 11~13번. Pages 를 켠 뒤 **`Re-run all jobs`** 를 누르세요 |
| 로그인 버튼을 눌러도 아무 일이 없음 | 32번 주소가 틀렸습니다. **뒤에 `/` 나 경로가 붙으면 안 됩니다** |
| 로그인은 되는데 저장이 안 됨 | 22~27번 권한, 특히 **`저장`을 두 번** 눌렀는지 보세요 |
| 「폴더 고르기」를 눌러도 창이 안 뜸 | 19번(Picker API)과 35번(API 키)을 보세요 |
| 예전 폴더가 「연결 실패」 로 뜸 | 권한을 좁힌 뒤라 그렇습니다. **「폴더 고르기」로 그 폴더를 한 번만 다시** 골라 주세요 |
| 손으로 넣은 파일이 「가져오기」에 안 잡힘 | 정상입니다. **「드라이브에서 파일 고르기」로 한 번 골라** 주면 그 뒤로는 잡힙니다 |
| 「액세스 차단됨」 | 21번 게시 상태가 `프로덕션` 인지, 사용자 유형이 `외부` 인지 보세요 |
| 고쳤는데 그대로임 | 배포에 1~2분 걸립니다. `Ctrl+Shift+R` 로 새로고침해 보세요 |
| `⬇ 앱으로 설치` 가 안 보임 | 이미 설치했거나 크롬·엣지가 아닙니다. 새로고침 뒤 잠시 기다려 보세요 |

---

# 코드를 고친 뒤 배포하기

깃허브에서 파일을 고치고 `Commit changes` 를 누르면 **자동으로 다시 배포됩니다.**
`Actions` 탭에서 초록 ✓ 가 되면 반영된 것입니다.

---

# 100명 한도는 없어졌습니다

예전 이야기부터 적어 둡니다. 이 앱은 한동안 `.../auth/drive`(드라이브 전체) 권한을 썼고,
구글은 심사받지 않은 앱에 **프로젝트 전 기간을 통틀어 100명** 한도를 걸었습니다.
그래서 한 사본이 100명을 채우면 사본을 새로 만들어야 했습니다.

지금은 **`.../auth/drive.file` 만** 씁니다. 민감하지 않은 범위라
**심사도, 인원 한도도 없습니다.** 사본을 몇 명에게 나눠 주어도 됩니다.

## 대신 규칙이 하나 있습니다

| | 보이나 |
|---|---|
| 앱이 만든 기록·폴더·사진 | **언제나 보입니다** |
| 고르기 창에서 한 번 고른 파일 | **그 뒤로 계속 보입니다** |
| 손으로 넣어 두기만 한 파일 | **안 보입니다** — 한 번 골라 주어야 합니다 |

폴더를 골라도 **그 안에 이미 있던 파일까지 권한이 퍼지지는 않습니다.** 직접 시험해 확인한 사실입니다.

그래서 밖에서 만들어 온 파일을 가져올 때는
`설정 → 📥 가져오기 → 「📂 드라이브에서 파일 고르기」` 로 한 번 골라 주세요.
**한 번 고른 파일은 그 뒤로 저절로 잡힙니다.** 매번 고를 필요는 없습니다.

> 📨 이미 만들어 둔 사본의 링크만 나눠 줄 때, 받는 사람에게 그대로 보낼 안내문은
> **[사용안내.md](사용안내.md)** 에 있습니다.
