# 🔖 작업 이어가기

> 이 문서는 **«지금 무엇을 해야 하나»** 만 적습니다. 짧게 유지하세요.
> «어쩌다 이렇게 됐나» 는 [작업기록.md](작업기록.md) 에 있습니다.
> 마지막 정리 **2026-08-26** · `main` 배포 완료 · 점검 **491가지** (도구 여덟) **+ 대비 전 조합** 전부 통과

---

## 🆕 이번 판에 바뀐 것 · 한 장 요약

앱이 **업노트(UpNote) 를 닮은 3단 도구**가 되었습니다. 큰 줄기 다섯입니다.

| | 무엇 | 어디 |
|---|---|---|
| ① | **3단** · 왼쪽 기둥 │ 가운데 목록 │ 오른쪽 편집 | `.layout` · `#listcol` · `.wrap` |
| ② | **쓰는 블록 다섯** · 할 일 · 목록 · 코드 · 표 · 접이식 + 글 안 서식 | `BLOCK_META` · `blocksToMarkdown` |
| ③ | **왼쪽 기둥** · 모든 기록 / 할 일 / 분류 없음 · 빠른 접근 · 폴더 · 태그 · 휴지통 | `renderSideNav` |
| ④ | **휴지통** · 지우기가 두 걸음이 됨 | `removeEntry` → `purgeNow` |
| ⑤ | **모양** · 흰 종이 + 청록(`clean`) · 프리텐다드 · 장식 없음 | `[data-tone="clean"]` |

---

## ▶ 지금 바로 할 일 · 사람이 눌러 봐야 하는 것

**코드는 다 되어 있습니다.** 다만 **로그인이 필요한 길에는 자동 점검이 한 발도 못 들어갑니다.**

| | 무엇 | 볼 것 |
|---|---|---|
| ① | **할 일 체크** | 목록에서 체크 → 드라이브의 `.md` 가 `- [x]` 로 바뀌는가 |
| ② | **지우기 두 걸음** | 삭제 → **드라이브 파일이 그대로인가** → 휴지통에서 완전히 삭제 → 그때 정리되는가 |
| ③ | **파일 옮기기** | 드라이브에서 연결 폴더 밖으로 옮긴 뒤 `🗂 폴더 구조 → 다시 읽기`.<br>「폴더 **밖**에 있습니다 · 문제없습니다」(회색) 인가, 「안 보입니다」(노랑) 인가 |
| ④ | **게시 끄기** | `②/③ 구글 문서` 를 `🔒 확실히 끄기` 로 끈 뒤 시크릿 창에서 안 열리는가 |
| ⑤ | 📥 자료 끌어오기 | **작은 폴더로 먼저** |
| ⑥ | 색감 일곱 · 폰 어둡게+아주 크게 | 한 번씩 |

> ⚠️ **되돌릴 수 없는 것은 ④의 링크 퍼짐뿐입니다.** 나머지는 다 되돌아갑니다.

### ③ 이 왜 중요한가 (아직 아무도 안 재 봤습니다)

`drive.file` 권한이 **파일을 옮겨도 살아남는지** 를 문서로는 알 수 없어 **앱이 직접 물어보게** 해 두었습니다
(`probeStrays` → `files/{id}` 로 확인). **뜨는 배너가 곧 답입니다.** 결과가 나오면
[작업기록.md](작업기록.md) 와 README 에 «실측» 으로 적으세요.

---

## ❓ 사용자에게 확인받을 것 하나

**파일 이름에서 날짜를 뺐습니다.** 사용자가 「태그(폴더명)를 먼저 정하고, `폴더명_제목` 으로 저장되게」
라고 해서 기본 규칙을 이렇게 바꿨습니다.

```
전:  {날짜}_{태그}_{제목}   →  2026-08-26_연수_수업 회고.md
후:  {태그}_{제목}          →  연수_수업 회고.md
```

- **잃는 것** · 드라이브에서 날짜순 정렬이 안 됩니다. 같은 태그+제목이면 `-2`, `-3` 이 붙습니다
- **되돌리는 법** · `DEFAULTS.mdPattern` · `filePattern` 한 줄. 그리고 `자리와이름.mjs` 의 ⑬⑭⑮⑰
- **이미 고친 사람은 안 건드립니다** · `normalizeSettings` 의 `OLD_DEFAULTS` 가 «옛 기본값 그대로인 사람» 만 옮깁니다

---

## ⛔ 건드리기 전에 반드시 읽을 것

### 0. 화면을 고쳤으면 **점검 아홉을 다 돌리세요** ← 이번 판에서 가장 크게 데인 곳

화면을 크게 바꿔 놓고 «자주 쓰는» 둘(`화면점검`·`구석구석`)만 고쳤습니다.
**셋이 조용히 빨간 채로 있었습니다** (`자리와이름` 67→52 · `자료가져오기` 71→크래시 · `색인쪼개기` 22→20).
앱 결함이 아니라 **점검이 옛 화면을 보고 있던** 것이었지만, 그걸 몰랐다는 게 문제였습니다.

```bash
for f in 화면점검 구석구석 자리와이름 자료가져오기 색인쪼개기 공유쪽 이름바꾸기; do node "docs/점검도구/$f.mjs" http://localhost:8123/ | tail -1; done
```

**특히 이 셋을 건드렸으면 반드시:**

| 건드린 것 | 돌릴 것 |
|---|---|
| 카드 단추 · 지우기 · 기둥 | `자리와이름` · `자료가져오기` · `색인쪼개기` |
| 저장 키 (`trace.*`) | `이름바꾸기` |
| `saveIndex`·`loadIndex` | `색인쪼개기` |
| 색 · 모양 | `대비점검` |

### 0-2. 자동 점검은 «보이는 것» 을 못 본다

이번 판에서 **자동 점검을 다 통과한 결함이 셋** 나왔습니다.

| 결함 | 왜 안 잡혔나 |
|---|---|
| 「＋ 새 기록」 이 눌리지 않음 | `.click()` 은 투명 덮개가 있어도 눌린다. **좌표로** 눌러 봐야 안다 |
| 폰에서 첫 기록까지 700px | 「단추가 있는가」 는 통과한다. **찍어서 봐야** 안다 |
| 머리띠만 가운데 떠 있음 | 요소는 다 있다. **찍어서 봐야** 안다 |

**UI 를 건드렸으면 화면을 찍어 눈으로 보세요.** 헤드리스로 `Page.captureScreenshot` 을 찍어
`Read` 로 보면 됩니다. 좌표 클릭은 `Input.dispatchMouseEvent` + `elementFromPoint`.

### 1. 클래스 이름은 «누가 이미 쓰고 있나» 로 정한다

작성칸을 접으며 `collapsed` 를 붙였더니, 이미 그 이름을 쓰던 **«접힌 본문의 페이드»**
(`.collapsed::after`) 가 70px 짜리 투명 덮개를 「＋ 새 기록」 위에 얹었습니다.
**눈에는 멀쩡한 단추가 보이는데 눌러도 아무 일이 안 일어났습니다.** 지금은 `cfolded` 입니다.

### 2. 새 블록 갈래를 만들면 «네 곳» 에 마저 적는다

| 어디 | 안 적으면 |
|---|---|
| `BLOCK_META` | 편집 화면 이름표가 전부 「글」 로 보인다 |
| `blocksToMarkdown` | 드라이브의 `.md` 에서 빠진다 |
| `blocksToHtml` | HTML 내보내기에서 빠진다 |
| `buildSharePacket` + `view.html` | **공유한 쪽에서 통째로 사라진다 · 오류도 없이** |

표가 든 수업 계획을 보냈는데 받는 쪽에 표가 없던 것이 실제로 이 자리였습니다.

### 3. 버린 것은 «어디에서도» 없는 것처럼 군다

`n.trashed` 를 세운 뒤 **목록에서만 빼면 안 됩니다.** 아래 다섯이 전부 `liveEntries()` 를 써야 합니다.

`renderSideNav` · `linkData`(백링크) · `buildGraph`(관계망) · 태그 칩 셈 · `syncPending`(올리기)

⚠️ **`syncPending` 이 특히 중요합니다.** 안 빼면 버린 글이 드라이브에 **다시 쓰입니다.**

### 4. `deskId` 를 `folderId` 에 걸지 않는다

`folderId` 는 «기록 하나가 통째로 가진 폴더» 라 **지우기가 그리로 향합니다.**
여러 기록이 함께 쓰는 자리를 거기 걸면 **한 편을 지웠을 때 폴더가 통째로 날아갑니다.**
자리는 `deskId`·`deskName`·`deskPath` 라는 딴 칸에 들고, 지우기는 그 칸을 안 봅니다.

### 5. 「안 보인다」 를 「지워졌다」 로 읽지 않는다

`drive.file` 에서 «안 보인다» 는 «권한이 끊겼다» 일 수도 있습니다.
**저절로 지우면 권한이 잠깐 흔들린 날 십삼 년치가 통째로 사라집니다.**
그래서 ① 파일 ID 로 직접 물어보고 ② 살아 있으면 「밖에 있다」(회색) ③ 못 열 때만 「안 보입니다」(노랑)
④ 치우는 것은 **사람이** 합니다 (`목록에서 치우기` → 휴지통).

### 6. 한 자리를 둘이 나눠 갖지 못하면 «때» 를 나눈다

폰에서 아래 바와 「넣기 줄」 이 둘 다 바닥에 붙으려다 46px 이 물렸습니다.
sticky 를 74px 올려도 **부모 상자 밖으로는 못 나가서** 그 값을 못 지켰습니다.
지금은 **쓰는 동안 아래 바가 물러납니다** (`body.writing`).

### 7. 이름은 화면이 아니라 «자료» 에도 박혀 있다

`PKEMS` → `PEER` → `TRACE`. 잘못 건드리면 **쓰던 사람의 기록이 통째로 없는 것이 됩니다.**

| 어디 | 지금 | 이어받는 법 |
|---|---|---|
| 드라이브 | `TRACE-index.json` · `TRACE-settings.json` | `findOrInherit()` · **파일 ID 는 그대로, 이름만** |
| localStorage | `trace.*` | `inheritOldKeys()` · **옛 키는 안 지운다** |
| IndexedDB | **`pkems` 그대로** | 아직 못 올린 사진이 들어 있다 |

### 8. 색인은 커지면 조각으로 갈린다

256KB 를 넘으면 목차 + `TRACE-index-00.json`… 으로 갑니다.
**조각을 못 읽으면 저장을 멈춥니다** (`indexIncomplete`). 반쪽으로 덮어쓰면 그 조각이 통째로 지워집니다.

### 9. 자리와 이름은 앱이 정하지 않는다

앱이 정하는 것은 **새로 만드는 것** 뿐. 이미 드라이브에 있는 것은 **드라이브가 진실**입니다.

| 무엇 | 앱이 만든 것 | 밖에서 온 것 (`srcId`) |
|---|---|---|
| 자리 | 정리 방식대로 | **사람이 둔 곳 그대로** |
| 이름 | 이름 규칙대로 | **사람이 붙인 그대로** |
| 지우기 | 파일도 휴지통으로 | **목록에서만 뺀다** |

### 10. 설명을 붙이지 마세요 · 긴 줄표를 쓰지 마세요

쓰기 전에 읽어야 할 글이 있으면 사람들은 그냥 안 씁니다.
남길 것은 «되돌릴 수 없는 것» 과 «안 되는 것» 뿐, 그것도 한 줄로, 그때만.
문장 끝이면 마침표, 그 밖에는 가운뎃점(·).

---

## 🕳️ 함정 모음 · 그냥 지나가세요

| 함정 | 어떻게 피하나 |
|---|---|
| **띄우기가 죽은 줄 모르고 도구를 의심한다** | 점검이 줄줄이 이상하면 먼저 `curl -s -o /dev/null -w "%{http_code}" http://localhost:8123/` |
| **8000 포트가 윈도우에 막혀 있다** | `띄우기.ps1 -Port 8123` · `.claude/launch.json` 도 8123 |
| **점검을 잇달아 돌리면 가끔 흔들린다** | 도구마다 CDP 포트가 고정이라 앞 브라우저가 안 죽으면 겹칩니다. 빨간 줄이 뜨면 **그 도구만 다시** 돌려 보세요 |
| **점검이 «되던 일» 을 부순다** | 지우기 점검이 기록을 진짜로 지우면 뒤따르는 열여덟이 볼 것을 잃습니다. **물음만 읽고 되돌려 놓으세요** |
| **점검이 옛 색감 이름을 못 박아 둔다** | `shareWebTone = 'craft'` 같은 것. 태어날 때의 색이 바뀌는 날 빨개집니다. **지금 서 있는 색을 읽어 쓰세요** |
| **셸이 백틱·백슬래시를 먹는다** | 정규식·경로가 든 치환은 **Edit 도구**로. python 의 `\\\\` 는 두 번 먹힙니다 |
| **`index.html` 은 CRLF** | 여러 줄 치환은 **Edit 도구**로 |
| **`var()` 는 «선언한 곳» 에서 풀린다** | 카드마다 달라지는 값은 덮개(`--veil`)를 걷는 방식으로 |
| **`<button>` 은 쪼갤 수 없는 덩어리** | 카드 제목을 단추로 만들었더니 긴 제목이 줄바꿈을 못 해 폰에서 39px 넘쳤습니다. 글처럼 흘러야 하면 `span[role=button]` |
| **한 값을 두 곳에 적으면 어긋난다** | `.addbar` 의 bottom 을 두 미디어 블록에 적었다가 뒤엣것이 앞엣것을 덮었습니다. `--dockh` 처럼 한 곳에 |
| **`▾` 같은 글자는 한글 글꼴에서 «-» 로 보인다** | 손잡이는 SVG(`caretSvg()`) 로 |
| **로그인 뒤에만 보이는 화면은 점검이 못 간다** | 캡처를 받아 **눈으로** 보세요 |

---

## 🧪 점검 도구

```bash
powershell -ExecutionPolicy Bypass -File docs/점검도구/띄우기.ps1 -Port 8123
```

| 도구 | 가짓수 | 언제 |
|---|---|---|
| `화면점검.mjs` | **236** | 화면을 고칠 때마다 |
| `자료가져오기.mjs` | **75** | 가져오기·공유·가리기·«안 보임» 을 건드리면 |
| `자리와이름.mjs` | **67** | **자리·이름·지우기·기둥을 건드리면 반드시** |
| `구석구석.mjs` | **33** | 원래 있던 화면을 건드리면 |
| `소개페이지.mjs` | 27 | `intro.html` 을 고치면 (`… /intro.html <출력폴더>`) |
| `색인쪼개기.mjs` | 22 | **`saveIndex`·`loadIndex` 를 건드리면 반드시** |
| `공유쪽.mjs` | 20 | `view.html` 을 고치면 |
| `이름바꾸기.mjs` | 11 | **저장 키를 건드리면 반드시** |
| `대비점검.mjs` | 전 조합 | **색을 건드리면 반드시** · `<출력폴더> <주소>` 순서 |
| `색감만들기.mjs` | (만드는 도구) | 손으로 CSS 를 고치지 말 것 |

---

## 📌 아직 안 한 것

| 무엇 | 왜 미뤘나 |
|---|---|
| **버전 기록** | 업노트에 있고 드라이브에도 이미 있습니다(`revisions`). **로그인 없이 시험을 못 해서** 손대지 않았습니다 |
| **휴지통 자동 비우기** | 업노트는 30일 뒤 지웁니다. 지금은 손으로만 비웁니다 |
| **잠금 · 공간(Spaces) · 노트북 표지** | 잠금은 브라우저 저장소라 «진짜 잠금» 이 안 됩니다. 공간은 «폴더 하나에 연결» 이라는 뿌리를 흔듭니다 |
| `localStorage` 5MB 벽 | 1104편에서 `saveLocal()` 이 조용히 접혔습니다. IndexedDB 로 옮길 일입니다 |
| SETUP 캡처 넉 장 | `setup-01`~`04` 에 옛 저장소 이름이 찍혀 있습니다. 사용자가 탭을 앞에 세워 줘야 합니다 |
| 보여 주는 판 (Artifact) | <https://claude.ai/code/artifact/01a03979-06f9-4304-ba28-ad23fd709fbb> · **지금 화면과 아주 다릅니다.** 3단·업노트 모양으로 다시 그릴 일입니다 |
