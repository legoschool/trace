# 🔖 작업 이어가기 (새 세션용 인수인계)

> 📌 **이 문서는 «지금 무엇을 해야 하나» 입니다.**
> «어쩌다 이렇게 됐나» 는 옆 문서에 있습니다 — **[작업기록.md](작업기록.md)**.
> 처음 넘겨받았다면 그쪽을 먼저 읽는 편이 빠릅니다. 이레 동안 걸어온 길이 한 장에 있습니다.

## 📝 이름 — 도구는 「구글 기록장」, 체계는 「TRACE」

**TRACE** — **T**eaching · **R**ecord · **A**rchive · **C**onnection · **E**xperience

> ⚠️ **둘을 섞지 마세요.** 사람이 쓰는 도구 이름은 **구글 기록장**입니다.
> **TRACE** 는 그 도구가 속한 **체계(관리 체계)** 이름입니다.
> 구글 로그인 동의 화면에 뜨는 이름도 **「구글 기록장」** 이고, 그대로 두는 것이 맞습니다.
>
> 이름이 두 번 바뀌었습니다: **PKEMS → PEER → TRACE.**
> 이어받기는 **사슬**로 되어 있습니다 — 새 이름이 비면 `peer.` 를 보고, 그것도 없으면 `pkems.` 를 봅니다.

### ⚠️ 이름을 더 손댈 때 반드시 알아야 할 것

이름은 **화면에만 있는 게 아니라 자료에도 박혀 있습니다.** 여기를 잘못 건드리면
**쓰던 사람의 기록이 통째로 «없는 것»이 됩니다.** 화면 결함이 아니라 자료 사고입니다.

| 어디 | 지금 | 어떻게 이어받나 |
|---|---|---|
| 드라이브 | `TRACE-index.json` · `TRACE-settings.json` | `findOrInherit()` — 옛 이름들을 **최근 것부터** 찾아 하나라도 있으면 **파일 ID 는 그대로 두고 이름만** PATCH 로 바꾼다. 새로 만들지 않으므로 안에 든 것이 안 움직인다. 이름 바꾸기가 실패해도 그 파일을 그대로 쓴다 |
| localStorage | `trace.*` | 시작할 때 `inheritOldKeys()` 가 `peer.*` → `pkems.*` 순으로 찾아 옮겨 담는다. **옛 키는 지우지 않는다** (되돌릴 수 있게) |
| IndexedDB | **`pkems` 그대로** | 여기엔 «아직 못 올린 사진」이 들어 있다. 이름을 바꾸면 그게 사라진다. 화면에 안 보이는 이름이라 **처음 이름 그대로** 둔다 |
| 공유 캐시 | `trace-share-inbox` | 스쳐 가는 것이라 그냥 바꿨다. **`index.html` 과 `sw.js` 양쪽이 같아야 한다** |

**점검**: `node docs/점검도구/이름바꾸기.mjs` — 옛 이름으로 채워 놓고 열어서
기록·폴더·설정·쓰다 만 글이 다 따라오는지 본다. 9가지.

### 저장소·폴더 이름도 바꿨습니다

| | 전 | 후 |
|---|---|---|
| GitHub 저장소 | `legoschool/pkems-googlenote` | **`legoschool/trace`** |
| 배포 주소 | `legoschool.github.io/pkems-googlenote/` | **`legoschool.github.io/trace/`** |
| 로컬 폴더 | `D:\구글 기록장\pkems_구글 기록장\` | **`D:\구글 기록장\trace_구글 기록장\`** |

> ⚠️ **배포 주소가 바뀌었습니다.** 전에 링크를 받아 간 사람이 있으면 새 주소를 다시 줘야 합니다.
> GitHub 은 저장소 주소는 옛것에서 새것으로 넘겨주지만, **Pages 주소는 보장하지 않습니다.**

> ⚠️ **`SETUP.md` 의 캡처 36장에는 옛 저장소 이름이 찍혀 있을 수 있습니다.**
> 안내 글은 고쳤지만 그림 속 글자는 못 고칩니다. 다시 찍을 때 함께 맞추세요.

> 로컬 폴더를 바꾸면 **띄우기 스크립트 경로**도 따라가야 합니다.
> `.claude/launch.json` 이 이제 저장소 안의 `docs/점검도구/띄우기.ps1` 를 가리키고,
> 그 스크립트는 `$PSScriptRoot` 기준이라 폴더를 또 옮겨도 알아서 따라갑니다.

> 마지막 정리: **2026-08-23 (v7 — 축 넷)** · 지난 묶음은 `f5166de` · `ffd924d` · `8841674` · `e71aa55` · `cf391cb`
> · **v6 에서 색인을 조각으로 나눴습니다** (아래 「▶▶ v6」)

---

# ▶ 지금 바로 할 일 — 실제 계정으로 눌러 보기

**코드는 다 되어 있고 배포까지 끝났습니다. 남은 것은 «사람이 한 번 눌러 보는 일» 뿐입니다.**

점검 도구 214가지 + 미리보기 11 + 대비 28조합이 전부 통과했지만, **로그인이 필요한 길에는
자동 점검이 한 발도 못 들어갑니다.** 아래는 그 길들입니다. 시험용 폴더 하나로 20분이면 됩니다.

> 되돌릴 수 없는 것은 **③의 링크 퍼짐** 뿐입니다. 나머지는 다 되돌아갑니다.

| 순서 | 무엇 | 무엇을 봐야 하나 |
|---|---|---|
| ① | **폴더 찾아보기** | `설정 → 저장 위치 → 📂 폴더 찾아보기`. 진짜 드라이브의 `root` 를 읽는 것은 처음입니다. 폴더가 200개 넘는 계정에서 어떻게 되는지도 안 봤습니다 |
| ② | **녹음** | 제목 칸에 짧게 적고 저장 → **드라이브에서 파일 이름을 확인.** 이번에 고친 자리가 정확히 거기입니다 |
| ③ | **🌐 웹 쪽으로 공유** ← **가장 중요** | 사진 하나·녹음 하나 든 기록으로 만들고 **시크릿 창에서** 열 것. 볼 것 셋 — 사진이 보이는가 · 녹음이 재생되는가 · **「공유 끄기」 뒤에는 안 열리는가** |
| ④ | **폴더 구조 → 🔄 드라이브에서 다시 읽기** | 손으로 만든 빈 폴더가 나오면 맞습니다 |
| ⑤ | **📥 자료 끌어오기** | **작은 폴더로 먼저.** 원본을 안 건드리니 잘못 들어와도 목록에서 지우면 됩니다 |
| ⑥ | **모양 프리셋** | 폰에서 한 번. 블록놀이가 너무 유치한지, 설계도가 너무 밋밋한지 |

### ③에서 사진이 안 보이면 — 고장이 아닙니다

**«드라이브에서 열어 보기» 줄로 내려가 있을 것입니다.** 사진을 부르는 주소
(`lh3.googleusercontent.com/d/{id}`)는 **구글이 문서로 약속한 길이 아닙니다.** 막힐 때를 대비해
두 겹을 뒀습니다 — 다른 주소(`drive.google.com/thumbnail`) → 그것도 막히면 링크.
그 줄이 보이면 **대비책이 도는 것**이니, 그때 다른 주소로 바꿔 보면 됩니다.

### 📸 SETUP 캡처 — 넉 장만 다시 찍으면 됩니다 (하다가 멈춘 자리)

**「36장을 다시 찍어야 한다」는 앞선 인수인계서의 말은 틀렸습니다.** 40장을 다 열어 확인했습니다.

| 장 | 상태 |
|---|---|
| `setup-01` `02` `03` `04` | ❌ **낡음.** 원본 저장소 이름 `pkems-googlenote` 가 주소창·제목·「Start with a template」 에 찍혀 있습니다 |
| `setup-05` ~ `setup-40` | ✅ **멀쩡함.** 여기서부터는 «읽는 사람 자기 저장소(`my-googlenote`)» 화면이라 원본 이름이 안 나옵니다 |

> 📌 **본문의 `my-googlenote` 는 낡은 것이 아닙니다.** 읽는 사람이 «자기 저장소에 지을 이름» 의
> 예시입니다. 고치지 마세요. 낡은 것은 **원본(템플릿) 저장소 이름이 찍힌 그림**뿐입니다.

**안내 흐름 자체는 멀쩡합니다** — `gh repo view legoschool/trace` 로 확인했습니다:
`isTemplate: true` · Public · 설명과 홈페이지 주소(`legoschool.github.io/trace/`) 모두 새것.
그래서 「Use this template」 단추가 그대로 나옵니다.

#### 왜 멈췄나 — 탭을 앞으로 못 세운다

캡처는 «바탕화면을 찍는» 방식이라 (주소창까지 나와야 05번 이후와 어긋나지 않습니다)
**찍으려는 탭이 크롬 맨 앞에 있어야 합니다.**

그런데 **브라우저 도구에는 «탭을 앞으로 세우는» 기능이 없습니다.**
`tabs_context` · `navigate` · `computer` · `read_page` · `javascript_tool` · `tabs_create` · `tabs_close` 뿐입니다.
배경 탭의 DOM 은 읽고 클릭도 되지만, 화면 앞으로는 못 나옵니다.
클릭해 봐도 `MainWindowTitle` 은 사용자가 보던 탭 그대로였습니다.

> ⚠️ 이걸 모르고 찍으면 **좌표는 맞는데 내용이 다른 사진**이 나옵니다.
> v4 에서도 같은 자리에서 한 번 당했습니다 (그때는 Claude 창이 찍혔습니다).

#### 다시 시작할 때 순서

1. **사용자에게 부탁합니다** — 크롬에서 `github.com/legoschool/trace` 탭을 눌러 **맨 앞에 세워 달라고**
2. 창을 고정합니다 (**두 번** 부를 것)
   ```powershell
   docs/캡처도구/movewin.ps1 -Title "legoschool/trace" -X 0 -Y 0 -W 2000 -H 1450
   docs/캡처도구/key.ps1 -Keys "^+b"      # 북마크바 숨김 — 개인 북마크가 찍힙니다
   ```
3. `docs/캡처도구/rect.js` 를 페이지에 넣고 `__t('Use this template')` 로 화면 좌표를 구합니다
4. `shotbox.ps1` 로 넉 장. **`-Front "legoschool/trace"` 를 빠뜨리지 마세요**
   | 장 | 화면 | 붉은 네모를 칠 곳 | 크기 |
   |---|---|---|---|
   | 01 | 저장소 첫 화면 | `Use this template` | 1990×900 |
   | 02 | 그 단추를 눌러 메뉴 펼친 상태 | `Create a new repository` | 1990×900 |
   | 03 | `github.com/new?template_name=trace&template_owner=legoschool` · 이름칸에 `my-googlenote` | 이름칸 | 1990×1180 |
   | 04 | 03 에서 아래로 굴린 화면 | `Public` · `Create repository` | 1990×1180 |
5. **`Create repository` 는 절대 누르지 마세요.** 양식만 찍고 나옵니다
6. 끝나면 **북마크바를 도로 켭니다** (`key.ps1 -Keys "^+b"`)

### 오래 남아 있는 숙제 (급하지 않음)

- **십삼 년치를 진짜로 끌어오기** — ⑤를 작은 폴더로 통과한 다음
- **📛 밖에서 넣은 파일 이름 정리** — 되돌리려면 손으로 해야 합니다. 시험용 폴더로 먼저
- **가져온 `.md` 를 고치면 원본이 옮겨지는 것** — 그대로 둘지 제자리에 둘지 **사용자가 정해야 합니다** (아래 「아직 그대로 둔 것」)

> ✅ **«색인이 커지는 것» 은 v6 에서 풀었습니다.** 이제 색인이 한 아름(256KB)을 넘으면
> 스스로 조각으로 나뉘고, **고친 기록이 든 조각 하나만** 다시 올라갑니다.
> ⑤를 할 때는 대신 이것을 봐 주세요 — **드라이브 폴더에 `TRACE-index-00.json` 같은 파일이 생기는지**,
> 그리고 그 뒤로도 **저장이 여전히 빠른지**. 자세한 것은 아래 「▶▶ v6」.

---

## 🖼️ 보여 주는 판 (Artifact) — 남에게 설명할 때

<https://claude.ai/code/artifact/01a03979-06f9-4304-ba28-ad23fd709fbb>

처음에는 «시안(A·B·C 세 갈래)» 이었는데, 일곱 갈래가 앱에 들어간 뒤
**«지금 이렇게 되어 있습니다»** 로 다시 그렸습니다. 판 셋입니다.

| 판 | 무엇 |
|---|---|
| 모양 일곱 갈래 | 아이답게 → 정밀하게 축. 각 갈래를 **그 갈래의 실제 값으로** 그렸다 |
| 남에게 보내는 길 넷 | ①②③④ 와 고르는 잣대 · 왜 ①을 따로 만들었나 |
| 부품 | 유형 여섯 자리 색 · 글꼴 · 손그림 아이콘 |

> 📌 **고칠 때는 새로 만들지 말고 같은 링크에 덮어쓰세요.** 원본은
> 스크래치패드의 `design/` 안에 `Main.dc.html` · `Share.dc.html` · `Parts.dc.html` · `canvas.json` 로 있습니다.
> 세션이 바뀌어 그 폴더가 없어졌으면, 위 주소를 `--extract` 해서 되받은 다음 고치면 됩니다.
> ⚠️ 앱을 고치면 이 판도 어긋납니다. 실제로 «세 갈래» 로 남아 있다가 한 번 어긋났습니다.

## 🕳️ 이번에 밟은 함정 모음 — 다음 사람은 그냥 지나가세요

같은 자리에 두 번 빠진 것도 있습니다. 코드가 아니라 **일하는 방식**의 함정입니다.

| 함정 | 어떻게 드러났나 | 어떻게 피하나 |
|---|---|---|
| **`var()` 는 «선언한 곳» 에서 풀린다** | `--entry-bg: var(--c-soft)` 로 짰는데 카드가 제 색을 못 입었다 | 카드마다 달라지는 값은 **카드에서** 풀리게 쓴다. 우리는 «덮개(`--veil`)를 걷는» 방식으로 뒤집었다 |
| **점검 잣대가 배경 «그림» 을 못 본다** | 멀쩡한 「기본」이 대비 미달로 나왔다 | `backgroundImage` 도 함께 본다 (`대비점검.mjs`) |
| **헤드리스 브라우저가 안 죽고 남는다** | 「＋버튼 0개」 같은 헛된 실패. 멀쩡한 코드를 의심했다 | 점검을 이어 돌리기 전에 포트 9xxx 짜리 msedge 를 죽인다 |
| **가짜 드라이브가 뜰 때마다 `localStorage` 를 비운다** | 새로고침이 필요한 점검을 짤 수 없었다 | `sessionStorage 'keep'` 빗장을 건다 (`자료가져오기.mjs`) |
| **셸 heredoc 이 백슬래시를 먹는다** | 정규식 `\.` 가 `.` 이 되어 아무 글자나 걸렸다. 파일 경로도 깨졌다 | 정규식·경로가 든 스크립트는 **Write 도구로** 쓴다. 셸을 거치지 않는다 |
| **파이썬 쓰기가 중간에 실패하면 파일이 0바이트가 된다** | 점검 파일 하나가 통째로 잘렸다 (커밋에서 되살림) | 한글이 든 파일은 **Write/node** 로 쓴다. `print` 인코딩도 실패 원인이 된다 |
| **점검이 엉뚱한 카드를 집는다** | 목록 맨 위에 사진 1100장이 깔려 있어 다른 기록의 줄을 눌렀다 | 검색으로 좁힌 뒤에 누른다 |
| **이름이 겹친다** | 「블록」을 찾다가 「블록**놀이**」가 먼저 잡혔다 | 이름 칸만 정확히 견준다 |
| **브라우저 도구는 탭을 앞으로 못 세운다** | 배경 탭의 DOM 은 읽히는데, 바탕화면을 찍으면 사용자가 보던 탭이 찍힌다 | 바탕화면을 찍어야 하면 **사용자에게 탭을 눌러 달라고** 부탁한다 |
| **공유 줄 이름이 겹쳤다** | 「웹 쪽으로」와 「웹 페이지로」— 화면만 봐서는 못 고른다 | 비슷한 것을 만들면 **나란히 놓고** 한 번 본다 |
| **띄우기가 죽은 줄 모르고 도구를 의심했다** | 점검 넷을 이어 돌리는 사이 서버가 죽었다. 「localStorage 접근 거부」·「탭을 못 찾음」 처럼 **도구가 낡은 것처럼** 보이는 오류가 났다. v6 에서 「소개페이지.mjs 가 낡았다」고 인수인계서에 잘못 적기까지 했다 (실제로는 27/27 통과) | 줄줄이 이상하면 먼저 `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/` 로 **서버부터** 본다. 200 이 아니면 도구 이야기가 아니다 |

---

# ▶▶ v7 (2026-08-23) — 색감을 모양에서 뗀다 [2/2]

[1/2] 에서 밝기·글자 크기를 갈랐고, 여기서 **색**을 갈랐습니다. 이제 축이 넷입니다.

| 축 | 붙는 곳 | 쥐고 있는 것 |
|---|---|---|
| **모양** | `data-theme` | 글꼴 · 모서리 여섯 · 테두리 굵기 · 돌기 · 여백 · 덮개 — **색은 한 톨도 없다** |
| **색감** | `data-tone` | 종이 · 잉크 · 강조 · 선 · 유형 여섯 · 알림 세 벌 — **모양은 한 톨도 없다** |
| **밝기** | `data-mode` | 기기 따름 · 밝게 · 어둡게 |
| **글자 크기** | `data-size` | 보통 · 크게 · 아주 크게 |

**「공책 모양 + 청사진 색 + 어둡게 + 아주 크게」** 가 됩니다.

## 색감 여섯

| 키 | 이름 | 어떤 색 | 어느 모양이 태어날 때 쓰나 |
|---|---|---|---|
| `craft` | 크래프트 | 크림 종이에 붉은 흙 | 메모지 · 블록 · 공책 |
| `sunny` | 볕 | 노란 종이에 빨강 | 블록놀이 |
| `sky` | 하늘 | 흰 종이에 파랑 | 기본 |
| `forest` | 숲 | 연둣 종이에 짙은 초록 | (새것) |
| `ink` | 먹 | 회백 종이에 먹 | 문서 |
| `blue` | 청사진 | 청회 바탕에 청록 | 설계도 |

**색감을 안 고르면 그 모양이 태어날 때의 색으로 섭니다** (`TONE_OF`).
그 짝이 곧 v6 까지의 프리셋 일곱이라, **쓰던 사람에게는 아무것도 안 바뀐 것처럼 보입니다.**

## ⚠️ 하나 달라진 것 — 어긋나 있던 것이 맞춰졌다

프리셋마다 팔레트가 **손으로** 적혀 있어 일곱 벌이 조금씩 어긋나 있었습니다.
한 벌의 규칙으로 다시 세우면서 **몇 곳의 색이 아주 조금 바뀝니다.**

- 「공책」의 바탕이 `#EFE7DA` → `#FBF6EE` (메모지·블록과 같은 크림으로)
- 「기본」·「문서」의 유형 색이 조금 짙어졌습니다

그래서 대비 수치가 **여러 곳에서 좋아졌습니다** —
기본 배지 `4.94 → 5.29`, 문서 배지 `5.1 → 6.26`, 공책 날짜 `6.11 → 6.16`.
가장 아슬아슬하던 자리가 올라간 것이라, 이 어긋남을 없앤 값이 있습니다.

## 지켜야 할 규칙 둘

> ⚠️ **모양 블록에 색을 적지 마세요.** 적고 싶어지면 그건 색감에 적어야 하는 것입니다.
> 딱 하나 예외 — 테두리를 «잉크색으로» 쓰는 모양(메모지·블록·블록놀이)은
> `--line: var(--ink)` 로 **가리키기만** 합니다. 색 자체를 적는 것이 아닙니다.
> 딱딱한 그림자도 마찬가지로 `var(--hard)` 를 가리킵니다.

> ⚠️ **색감의 «어두울 때» 값은 두 자리에 놓입니다** (기기 따름 · 사람이 고름).
> 손으로 두 벌을 쓰지 마세요. 한 벌만 쓰고 찍어 낸 것입니다 —
> 만드는 스크립트는 `docs/점검도구/색감만들기.mjs` 에 넣어 두었습니다.
> 고칠 일이 있으면 그 파일의 `TONES` 만 고치고 다시 돌리세요.

## 점검

**269가지 → 275가지 + 대비 47조합.** 전부 통과.

| 무엇 | 몇 |
|---|---|
| 모양 일곱 × 밝기 둘 (기존) | 14 |
| **색감 여섯 × 밝기 둘** — 가장 험한 모양(메모지)에서 | **12** |
| 축이 서로 안 흔들리는가 | 21 |

> 💡 **왜 메모지에서 색감을 재나.** 메모지는 덮개(`--veil`)가 투명이라
> 카드 바탕에 **유형 색이 그대로 드러납니다.** 글자가 그 위에 얹히므로,
> 대비가 깨진다면 거기서 먼저 깨집니다. 가장 험한 자리 하나로 여섯을 다 재는 편이
> 마흔두 조합을 다 도는 것보다 낫습니다.

`화면점검.mjs` (97→101) — 색감 여섯이 다 있는가 · 색감을 바꾸면 색이 바뀌는가 ·
**색감을 바꿔도 모서리·테두리가 그대로인가** · 「태어날 때의 색으로」 가 되돌리는가.

---


# ▶▶ v7 (2026-08-23) — 보는 것을 «축» 으로 가른다 [1/2]

## 왜

프리셋 일곱이 **모양과 색을 한 덩이로** 들고 있었습니다. 그래서

- 「공책은 좋은데 어둡게 보고 싶다」 를 할 수 없었습니다 — 어두운 화면은 **기기 설정을 따를 뿐**이었습니다
- 「글자가 작다」 를 할 수 없었습니다 — 크기가 CSS 에 못으로 박혀 있었습니다
- 프리셋을 하나 고칠 때마다 **다른 것이 흔들렸습니다** — 색과 모양이 같은 자리에 있어서

사용자가 준 디자인 시안 열셋(`Downloads/디자인 샘플`)이 한결같이 말하는 것도
**저명도 배경 · 고시인성 · 본문 최소 크기**였습니다. 슬라이드용 원칙이지만 뜻은 같습니다 —
**읽히는 것이 먼저**입니다.

## 무엇이 달라졌나

보는 것을 정하는 축을 **셋으로 갈랐습니다.** 하나를 고쳐도 나머지는 안 흔들립니다.

| 축 | 붙는 곳 | 무엇을 쥐고 있나 |
|---|---|---|
| **모양** | `data-theme` | 글꼴 · 모서리 여섯 · 테두리 굵기 · 돌기 · 여백 · 덮개 |
| **밝기** | `data-mode` | (없음)=기기 따름 · `light`=밝게 · `dark`=어둡게 |
| **글자 크기** | `data-size` | (없음)=보통 · `big`≈17px · `huge`≈19px |

> 📌 **색감은 아직 모양에 붙어 있습니다.** 그것까지 가르는 것이 [2/2] 입니다.
> 한 번에 다 갈면 일곱 갈래가 통째로 흔들리므로 나눠서 합니다.

### 밝기 — 고른 것이 기기를 이긴다

전에는 `@media (prefers-color-scheme: dark)` 하나뿐이라 «기기 따름» 밖에 없었습니다.
이제 같은 색이 두 자리에 놓입니다 —

1. 기기가 어두울 때, **단 「밝게」 를 고르지 않았을 때만** (`:not([data-mode="light"])`)
2. 사람이 **「어둡게」 를 골랐을 때** (기기와 무관하게)

> ⚠️ **두 벌을 손으로 쓰지 마세요.** 언젠가 어긋납니다.
> 한 벌만 쓰고 `docs/점검도구` 옆의 스크립트로 찍어 냈습니다.
> 다시 손댈 일이 있으면 **한쪽만 고치고 다른 쪽을 잊는 일**을 가장 조심해야 합니다.
> 점검이 그것을 잡습니다 — 「기기밝음+어둡게」·「기기어두움+밝게」 두 줄.

### 글자 크기 — 배율 하나로

화면 CSS 의 `font-size` **145곳**을 `calc(N px * var(--fs))` 로 바꿨습니다.
`--fs` 하나만 바꾸면 전부 따라옵니다.

**그러면서 바닥을 올렸습니다** — `11.5px` 24곳 · `10.5px` 2곳 · `10px` 1곳이 **12px 아래**였습니다.
그 밑은 읽으라고 둔 글자가 아닙니다.

> ⚠️ **내보내는 쪽(`htmlPage`·`EXPORT_TOKENS`)의 CSS 는 안 건드렸습니다.**
> 그쪽은 자바스크립트 «문자열» 이라 같은 규칙이 아닙니다. 바꾸는 스크립트가
> `<style>` 안쪽만 훑도록 자리를 잘라 두었습니다.

## 점검

**263가지 → 269가지 + 대비 21조합.** 전부 통과.

- `화면점검.mjs` (91→97) — 축 셋을 실제로 눌러 본다:
  밝기를 바꿔도 모양이 그대로인가 · 글자를 키워도 밝기가 그대로인가 ·
  셋을 다 만져도 모양이 끝까지 그대로인가
- `대비점검.mjs` — **「축이 서로 안 흔들리는가」** 표를 새로 냈다.
  프리셋 일곱마다 ①기기밝음+어둡게 ②기기어두움+밝게 ③폰에서 아주 크게.
  ③은 **가로로 넘치는지**까지 본다 — 글자를 키우면 제일 먼저 터지는 곳이다.

기존 대비 28조합도 그대로 통과했습니다. **색은 한 톨도 안 건드렸기 때문**입니다.

## 다음 [2/2] 에서 할 것

- **색감을 모양에서 뗀다** (`data-tone`). 그러면 「공책 모양 + 청사진 색 + 어둡게」 가 됩니다
- 색감이 서면 **대비 점검이 tone × mode 만 보면 됩니다** — 모양은 색과 무관해지므로
  조합 수가 오히려 줄어듭니다
- 지금은 프리셋마다 팔레트가 손으로 적혀 있어 **일곱 벌이 조금씩 어긋나** 있습니다.
  한 벌의 «규칙» 으로 다시 세우면 그 어긋남이 사라집니다

---


# ▶▶ v6.3 (2026-08-23) — 「📥 가져오기」 를 꺼내고, 태그와 TRACE 를 말로 적는다

## ① 「📥 가져오기」 가 제 칸이 되었다

**이 도구를 만든 진짜 목적이 「고급」 안에 묻혀 있었습니다.** 묻어 두면 그 길이 있는 줄도 모릅니다.

| 전 | 후 |
|---|---|
| `설정 → 고급 → 📥 폴더 훑어서 가져오기` | `설정 → 📥 가져오기 → 폴더 훑어서 가져오기` |

- 「📛 밖에서 넣은 파일 이름 정리」 도 이 칸에 함께 뒀습니다 — **가져온 뒤에 하는 일**이라
  같은 자리에 있어야 이어집니다 (폴더 구조 창에도 그대로 있습니다)
- 「고급」 에는 백업·연결 해제와 «사본을 지은 사람용» 접힌 칸만 남았습니다

> ⚠️ **길이 바뀌었으니 문서도 따라갔습니다** — `README.md` 네 곳, `사용안내.md` 한 곳,
> 점검 도구 `자료가져오기.mjs`·`구석구석.mjs`. 새로 문서를 쓸 때 「설정 → 고급 → 📥」 이라고
> 적지 마세요. **이제 없는 길입니다.**

> 📌 `README.md` 에 하나 더 틀린 것이 있어 함께 고쳤습니다 —
> 「HTML 파일로도 저장」 은 «고급» 이 아니라 **「파일 이름」** 칸에 있습니다.

## ② 태그가 «나중에 이어 주는 실» 이라는 것을 적었다

태그를 «지금 나누는 일» 로만 여기면 대충 적게 됩니다. 태그 칸 바로 아래에 한 줄로 적었습니다 —
같은 태그끼리 **한 번에 모아 보고**, 관계망에서 **한 덩어리로 묶여** 보이고,
태그 이름이 **드라이브 폴더 이름**이 될 수도 있다는 것.

## ③ 맨 아래에 TRACE 를 적었다

**도구 이름과 체계 이름을 갈라 적는 자리**입니다. 앞으로 TRACE 아래에 다른 도구가 더 놓일 예정이라,
지금부터 갈라 두지 않으면 나중에 둘이 엉킵니다.

- **TRACE** — 개인 지식·경험 관리 «체계». Teaching · Record · Archive · Connection · Experience
- **구글 기록장** — TRACE 를 구글 드라이브 위에서 쓰는 «도구». 지금 이 앱

> 💡 새 도구가 붙으면 `.tracetool` 줄에 한 줄씩 더하면 됩니다.
> 문구는 `index.html` 의 `<footer class="site">` 안에 있습니다.

## ④ 넣기 줄 — 그림글자를 붙이고 ＋ 를 앞으로 모았다

단추 열하나가 전부 「＋ …」 로 시작했다. ＋ 가 열한 번 되풀이되면 그것은 더 이상 표지가 아니다.

- **＋ 는 앞에 크게 하나만** 세웠다 (「＋ 넣기」). 누르는 것이 아니라 «이 아래는 다 넣는 것» 이라는 표지라 단추로 만들지 않았다
- 단추마다 **그림글자**를 붙였다 — 🔠 소제목 · ✍️ 글 · ❝ 인용 · 📸 캐처 · 🖼️ 사진 · 🖌️ 손 메모 · 🎙️ 녹음 · 📁 파일·영상 · 🔗 링크 · 🧩 코드 삽입 · ➖ 구분선
- 좁은 화면에서는 「넣기」 글자를 빼고 ＋ 만 남긴다. 한 줄이라도 더 단추가 서야 한다

> 💡 **헷갈리기 쉬운 두 쌄을 갈랐습니다** — 📸 캐처(직접 찍는 것) / 🖼️ 사진(이미 있는 것),
> ✍️ 글(치는 것) / 🖌️ 손 메모(긋는 것). 점검이 이 네이 서로 다른지 재고 있습니다.

> ⚠️ 폰에서는 「캐처」 가 「📸 캐처·촬영」 로 바뀌는데(`isTouchDevice()`), 그쪽에도 그림글자가 들어 있습니다.

---

## 점검

**259가지 → 263가지, 전부 통과.** 넣기 줄에 넷을 더했고, **낡아진 둘을 고쳤습니다** —
`자료가져오기.mjs` 가 「고급」 탭을 눌러 가져오기를 찾던 것, `구석구석.mjs` 의 「설정 탭이 다섯」.

---


# ▶▶ v6.2 (2026-08-23) — 써 보면서 나온 다섯 개 더

v6.1 을 올리고 이어서 써 보았습니다. 다섯이 또 나왔습니다.

| # | 무엇 | 어디 |
|---|---|---|
| ① | 「엮어내기」 창의 **바닥줄이 목록 위에 겹쳐** 보인다 | 모든 창(CSS) |
| ② | 뽑아낸 `.html` 이 **고른 모양을 안 따른다** | `htmlPage()` |
| ③ | 설정의 「웹 캡처」 칸이 **복잡하다** | 설정 |
| ④ | 설정 「고급」 이 **누구 것인지 안 갈린다** | 설정 |
| ⑤ | 「쪽 링크 만들기」 가 **낯선 말이다** | 공유 창 |

---

## ① 창이 깨져 보인다 — 한 줄짜리 CSS

`.modal` 은 `max-height: 88vh` 의 flex 기둥입니다. 그런데 몸통(`.mbody`)에
**`min-height: 0` 이 없었습니다.** flex 안에서는 그게 없으면 칸이
«내용만큼» 자리를 차지하겠다고 버팁니다. 그래서 **바닥줄이 창 밖으로 밀려나** 겹쳐 보였습니다.

기록이 몇 편일 때는 안 드러납니다. 목록이 길어지면 그때부터 보입니다.

> 💡 **flex 안에서 스크롤 되는 칸은 `flex:1 1 auto` 와 `min-height:0` 을 반드시 함께** 줘야 합니다.
> 맨 위·맨 아래에는 `flex:0 0 auto` 를 줍니다. 창 하나만의 문제가 아니라 모든 창의 문제였습니다.

## ② 뽑아낸 파일이 모양을 안 따른다

앱은 공책인데 엮어낸 `.html` 만 파란 업무 문서로 나왔습니다.
`htmlPage()` 가 색을 **말 그대로 박아** 놓고 있었습니다 (`--brand:#3f5bd9`).

이제 뽑아낸 파일이 `data-theme` 을 달고 나오고, 손글씨·명조 글꼴까지 함께 불러옵니다.

> ⚠️ **색 토큰이 이제 세 곳입니다** — `index.html` 화면 · `view.html` · `EXPORT_TOKENS`.
> 한 곳을 고치면 세 곳을 다 맞춰야 합니다. `EXPORT_TOKENS` 옆에 그 말을 적어 두었습니다.

## ③ 설정의 「웹 캡처」 칸을 뺐다

«복잡하고 쓰는 사람도 모르겠다» — 맞는 말입니다. 설명이 세 문단이었습니다.

- **공유 시트로 받는 길은 그대로 살아 있습니다** — `manifest.json` 의 `share_target` 이 맡습니다
- 함께 사라진 것은 **컴퓨터용 즐겨찾기 딱지(북마클릿)** 뿐입니다
- `bookmarkletCode()` 는 **안 지우고 두었습니다.** 받는 쪽(`intakeShared`)이 살아 있으므로
  이 한 줄만 어딘가에 다시 내주면 그날로 되살아납니다

## ④ 설정 「고급」 — 두 부류가 한 칸에 섞여 있었다

사용자가 «사본 배포할 때 사람들이 이걸 보고 알 수 있겠느냐» 고 물었습니다.
**그대로라면 어렵습니다.** 이유는 기능이 어려워서가 아니라 **듣는 사람이 둘이기 때문**입니다.

| 누구 것 | 무엇 |
|---|---|
| 쓰는 사람 | 폴더 훑어서 가져오기 · 다시 불러오기 · JSON 백업 · 연결 해제 |
| 사본을 지은 사람 | 클라이언트 ID · 앱 주소 (SETUP 때 한 번 쓰고 끝) |

이제 **쓰는 사람 것이 먼저** 서고, 관리자용 둘은 맨 아래에
「▸ 사본을 지은 사람용 설정」 으로 **접혀** 있습니다.

> 📌 **아직 남은 고민이 있습니다.** 「📥 폴더 훑어서 가져오기」 는 **이 도구를 만든 진짜 목적**인데
> «고급» 에 묻혀 있습니다. 밖으로 꺼내야 한다고 봅니다만, 그러면 README·사용안내·점검 여럿 곳의
> 「설정 → 고급 → 📥」 이 전부 따라가야 합니다. **→ v6.3 에서 꺼냈습니다.** 이제 제 칸입니다.

## ⑤ 「쪽 링크 만들기」 → 「URL …」

«쪽» 은 만든 사람의 말이지 쓰는 사람의 말이 아니었습니다. 익숙한 말은 **URL** 입니다.

| 전 | 후 |
|---|---|
| 🔗 웹 쪽 링크 만들기 | 🔗 **URL 링크 만들기** |
| 🔗 문서 쪽 링크 만들기 | 🔗 **문서 URL 링크 만들기** |
| 🔗 글 공유 링크 만들기 | 🔗 **문서 파일 URL 만들기** |
| 🔗 폴더 공유 링크 만들기 | 🔗 **폴더 URL 만들기** |

> ⚠️ **줄 제목(① 🌐 웹 쪽으로 …)은 그대로 두었습니다.** 그것이 넷을 갈라 주는 이름이고,
> README·점검이 그 이름으로 넷을 세고 있습니다. **누르는 단추만** 바꿨습니다.

---

## 🔍 사용자 화면에서 본 «우리 것이 아닌» 머리줄

뽑아낸 `.html` 을 열었더니 맨 위에 **「EduCreator 미스터리」 와 「홈」** 이 붙어 있었습니다.

**그것은 우리 파일에 없습니다.** 저장소 전체를 찾아도 없고,
뽑아낸 파일을 그 자리에서 받아 글자를 세어 봐도 없습니다
(점검 「뽑아낸 파일에 군더더기 머리줄이 없다」).
**크롬 확장이 끼운 것입니다.** 시크릿 창에서 열어 보면 없을 것입니다.

---

## 점검

**253가지 → 259가지, 전부 통과.** 이번에 늘어난 것은 여덟입니다.

| 도구 | 늘어난 점검 |
|---|---|
| `화면점검.mjs` (80→87) | 엮어내기 창의 겹침·화면 밖 · 뽑은 `.html` 의 모양·글꼴·군더더기 · 설정 칸·관리자 접힘 |
| `자료가져오기.mjs` (64→65) | 누르는 단추가 URL 이라는 말을 쓰는가 |

**낡아진 점검 셋도 뒤집었습니다** — 「설정에 웹 캡처 칸이 있다」·「즐겨찾기 한 줄이 만들어진다」·
「설정 탭이 여섯」. 이제 **«없어야 한다»** 를 봅니다.

---

# ▶▶ v6.1 (2026-08-22) — 실제로 써 보고 나온 여섯 가지

**드디어 사용자가 진짜 계정으로 눌러 봤습니다.** 그리고 여섯 개가 나왔습니다.
전부 «만든 길» 이 아니라 **«쓰는 길»** 에서 나온 것입니다. 자동 점검 253가지가 다 통과하던
상태에서 나왔다는 것이 요점입니다.

| # | 무엇이 잘못돼 있었나 | 어디 |
|---|---|---|
| ① | 공유한 쪽에서 **녹음이 잘린 것처럼** 보인다 (`0.04 / Infinity:NaN`) | `view.html` |
| ② | 목록·폴더 구조가 **펴진 채로** 열려 한 화면에 안 들어온다 | `index.html` |
| ③ | 손으로 만든 폴더를 **못 읽어 온다** — 단추를 눌러야만 나온다 | `index.html` |
| ④ | 관계망이 보기 줄에 없어 **있는 줄도 모른다** | `index.html` |
| ⑤ | 공유한 링크가 **이 앱의 주소를 흘린다** | `view.html` |
| ⑥ | 「가리세요」 라고 일러 주는데 **가는 길이 막혀 있다** | `index.html` |

---

## ① 녹음이 잘린 것처럼 보인다

**파일이 상한 것이 아닙니다.** 브라우저로 녹음한 소리(webm)에는 «얼마나 긴지» 가 안 적혀
있습니다. `MediaRecorder` 가 그렇게 만듭니다. 그래서 재생기가 「Infinity:NaN」 을 띄우고,
막대를 끌어 옮길 수도 없어 **잘린 것처럼** 보입니다.

- 맨 끝(`currentTime = 1e101`)으로 한 번 보내면 브라우저가 길이를 세어 줍니다.
  세고 나면 제자리로 돌려놓습니다 — `fixAudioDuration()`
- 앱 안 재생기 두 곳(블록 미리보기·녹음 창)과 공유 쪽에 모두 넣었습니다
- 공유 쪽은 **«우리 재생기» 를 먼저** 씁니다. 드라이브가 파일을 안 내주면
  그때 드라이브 재생기(`iframe`)로 물러섭니다 — 사진이 쓰던 방식과 같습니다

> ⚠️ **여기가 이번에 유일하게 «진짜로는 못 본» 자리입니다.** 가짜 아이디로는 드라이브가
> 파일을 안 내주므로 점검은 **물러서는 쪽만** 봅니다. 우리 재생기가 실제로 뜨는지는
> 진짜 공유 링크를 한 번 열어 봐야 압니다. 안 뜨면 드라이브 재생기가 나오니 **전보다 나쁘지는 않습니다.**

## ② 펴진 채로 열린다

목록 보기의 폴더와 폴더 구조 나무가 **처음부터 펴져** 있었습니다. 폴더가 예닐곱만 되어도
한 화면에 안 들어와, **무엇이 있는지 도리어 안 보입니다.**

이제 **접힌 채로** 열고, 눌러야 그 안이 보입니다. 폴더 구조는 **뿌리만** 폅니다.

## ③ 손으로 만든 폴더를 못 읽어 온다

폴더 구조는 «색인으로 그린 예상도» 였습니다. 진짜 드라이브를 읽으려면
「🔄 드라이브에서 다시 읽기」 를 눌러야 했습니다. 그래서 손으로 만든 `2026-테스트-08-21`
같은 폴더가 **«안 보인다 = 없다»** 로 읽혔습니다.

이제 **창을 열면 그 자리에서 드라이브를 읽습니다.** 못 읽으면 예상도를 그대로 두고
왜 못 읽었는지만 말합니다.

## ④ 관계망이 보기 줄에 없다

관계망은 머리줄 구석에만 있었습니다. **나열 · 바둑판 · 목록 · 관계망** 으로 같은 줄에 세웠습니다.

> 💡 앞의 셋은 «목록을 어떻게 그리나» 라서 골라 두면 남습니다.
> 관계망은 **그 자리에서 열어 보는 것** 이라 남지 않습니다 (`modal: true`).
> 그래서 누르고 닫으면 앞서 고른 보기로 돌아옵니다. 저장된 설정으로도 받아들이지 않습니다.

## ⑤ 공유한 링크가 앱 주소를 흘린다

공유한 쪽 아래에 **「구글 기록장이란?」** 링크가 있었습니다. 그 링크가 이 사본이 어디 있는지를
받는 사람에게 그대로 알려 줍니다. **로그인이 평생 100명 한도** 인 자리라,
기록 하나를 보여 주자고 걸 만한 것이 아닙니다. 링크를 뺐습니다.

## ⑥ 「가리세요」 라고 하는데 길이 막혀 있다

**이번에 나온 것 중 가장 나쁜 것입니다.** 공유 관문이 「얼굴이 보이면 편집 → 가리기 로
지우세요」 라고 일러 주는데, **정작 그 길이 두 겹으로 막혀 있었습니다.**

1. 편집 화면에서 **올라간 사진에는 「🔲 가리기」 단추가 아예 안 붙었습니다**
   (`!b.fileId && b.blob` — 올라가기 전 사진만)
2. 어찌어찌 불러도 `openMaskForBlock` 이 **「이미 드라이브에 올라가 있어 가릴 수 없습니다」** 로 막았습니다

그런데 **덮어쓰는 길(`_remask`)은 처음부터 있었습니다.** 「사진 가리기(여러 장)」 화면은
올라간 사진을 잘 고쳤습니다. 한 장짜리 길만 막혀 있었던 것입니다.

- 이제 올라간 사진에도 단추가 붙고, **드라이브에서 받아 와 고쳐 같은 파일에 덮어씁니다**
- 덮어쓸 때 **파일 이름은 그대로 둡니다** — 이름이 바뀌면 글 속 링크가 어긋납니다
- 관문에도 **「🔲 지금 가리기」** 를 두었습니다. 창을 닫고 편집을 찾아 들어갈 필요 없이,
  얼굴을 발견한 그 자리에서 바로 가립니다

> 🕳️ **여기서 배운 것** — 「하세요」 라고 적어 둔 길은 **그 길을 실제로 걸어 봐야** 합니다.
> 안내문은 멀쩡했고 기능도 있었는데, **둘을 잇는 자리가 끊겨** 있었습니다.
> 안내문을 쓸 때 그대로 따라 해 보는 것이 점검입니다.

---

## 점검

**253가지 — 전부 통과.** 이번에 늘어난 것은 열넷입니다.

| 도구 | 늘어난 점검 |
|---|---|
| `자료가져오기.mjs` (55→64) | 폴더 구조 자동 갱신 · 뿌리만 펴기 · 올라간 사진 가리기 · 관문의 «지금 가리기» · 목록 접힘 |
| `공유쪽.mjs` (16→19) | 우리 재생기 / 물러서기 · 녹음 옆 «열어 보기» · **앱 주소를 안 흘린다** |
| `구석구석.mjs` (27→30) | 보기 줄의 관계망 · 열리는가 · 고른 보기가 그대로인가 |
| `화면점검.mjs` (79→80) | 폴더 구조가 접힌 채로 열리는가 |

> 💡 **가짜 드라이브가 이번에 «진짜 그림» 을 내주게 됐습니다.**
> 전에는 사진 요청에 빈 몸통을 돌려주었습니다. 그러면 「올라간 사진을 가리기」 를
> 겪어 볼 수 없습니다 — 그림이 안 열려 거기서 끝나니까요. 8×8 짜리 PNG 를 물려 두었습니다.

### 점검을 고친 것도 다섯

목록·폴더 구조가 **접힌 채로** 열리게 바뀌자, 그것을 «펴진 것» 으로 알고 있던 점검 다섯이
빨개졌습니다. **도구가 낡은 것이지 앱이 깨진 것이 아닙니다.** 다 맞춰 두었습니다.

> ⚠️ 이런 것이 하나 더 있었습니다 — `자료가져오기.mjs` 에서 「올라간 사진을 가릴 수 있다」 가
> 계속 빨갰는데, 창 이름을 **「📸 캡처 영역 고르기」** 로 찾고 있었습니다.
> 실제로는 **「🔲 가릴 곳 고르기」** 입니다. **기능은 처음부터 되고 있었습니다.**
> 빨간 줄을 보면 앱부터 의심하게 되는데, 점검이 틀린 경우가 꽤 됩니다.

---

# ▶▶ v6 (2026-08-22) — 색인을 조각으로 나눈다

## 왜 했나

색인 하나(`TRACE-index.json`)에 기록을 **다 담고 있었습니다.**
그래서 📌 하나를 누르든 글자 하나를 고치든, **그 전부를 다시 올렸습니다.**
십삼 년치를 넣으면 색인이 몇 MB 가 되고, 폰에서는 그게 곧 「저장이 안 되는 도구」입니다.

인수인계서가 «진짜 위험» 이라고 적어 둔 유일한 항목이었고, 로그인 없이 고칠 수 있는
유일한 항목이기도 했습니다.

## 무엇이 달라졌나

| | 전 | 후 |
|---|---|---|
| 작을 때 | `TRACE-index.json` 하나 | **그대로** — 나누지 않는다 |
| 256KB 를 넘으면 | 그대로 (계속 커진다) | `TRACE-index.json` 은 **목차**가 되고, 기록은 `TRACE-index-00.json` · `-01.json` … 으로 갈린다 |
| 저장할 때 | **전부** 다시 올린다 | **바뀐 조각만** 올린다 |
| 더 커지면 | — | 조각 하나가 256KB 를 넘으면 조각 수를 **두 배로** 늘려 다시 나눈다 (최대 256조각) |
| 읽다 실패하면 | 조용히 «빈 색인» 을 돌려주었다 | **덮어쓰기를 멈추고** 사람에게 알린다 |

**측정값** — 기록 400편(색인 669KB)에서 하나를 고쳤을 때:
전에는 669KB 를 올렸고, 지금은 **169KB(조각 하나)** 만 올립니다.
3000편이면 16조각쯤 되어 차이는 더 벌어집니다.

## ⚠️ 가장 중요한 것은 나누는 게 아니라 «안 지우는» 것

나누는 순간 새로 생기는 사고가 있습니다. **조각 하나를 못 읽었는데 그걸 «빈 것» 으로 알고
저장하면, 그 조각에 든 기록이 통째로 지워집니다.** 그래서 세 겹을 뒀습니다.

1. **못 읽으면 저장을 멈춘다** — `indexIncomplete` 가 서면 `saveIndex()` 는 한 글자도 안 씁니다.
   화면에는 「덮어쓰지 않습니다 · 새로고침한 뒤 다시 연결해 주세요」 가 뜹니다
2. **조각을 먼저, 목차를 나중에** 올립니다. 중간에 끊겨도 목차는 옛것이라 짝이 안 어긋납니다
3. **옛 버전이 목차를 덮어써도 되살린다** — 기기 하나가 옛 버전이면 그 기기가
   `TRACE-index.json` 을 «통째로 든 색인» 으로 되돌려 놓습니다. 그래도 조각 파일은 폴더에 남아 있으므로,
   이름(`TRACE-index-`)으로 찾아 **함께 읽어** 합칩니다 (`readStrayShards`)

> 📌 **덤으로 옛 결함 하나를 막았습니다.** 전에는 `loadIndex()` 가 실패하면 조용히 `{entries:[]}` 를
> 돌려주었고, 곧바로 이어지는 `syncPending()` 이 그것을 저장했습니다.
> **신호가 잠깐 끊긴 것만으로** 드라이브의 색인이 «이 기기에 있는 것만» 으로 덮여 쓸 수 있었습니다.
> 조각과 상관없이 있던 구멍입니다.

## 어떻게 만들어졌나 (고칠 사람용)

전부 `index.html` 의 「7. 동기화」 안에 있습니다.

| 이름 | 하는 일 |
|---|---|
| `shardOf(id, n)` | 어느 조각에 넣을지. **기록 아이디로만** 정한다 — 글을 고쳐도 조각을 옮겨 다니지 않게 |
| `shardBody(i, n, arr)` | 조각 한 덩이의 글자. **안에서 줄을 세운다** — 들어온 차례가 달라도 같은 글자가 나와야 «안 바뀜» 을 알아볼 수 있다 |
| `buildShards()` | 조각별 글자를 만든다. 하나라도 너무 크면 조각 수를 두 배로 늘려 다시 짠다 |
| `fnv(s)` | 짧은 지문. 암호가 아니라 «바뀌었나» 만 본다 |
| `putShard(i, body)` | 조각 하나 올리기. 파일 ID 를 모르면 이름으로 먼저 찾는다 |
| `loadSharded(man)` | 목차를 보고 조각을 다 읽는다. 하나라도 못 읽으면 **반쪽 표시** |
| `readStrayShards(base)` | 옛 형식 옆에 남은 조각을 주워 담는다. 연결마다 한 번만 |

> 💡 **왜 «작을 때는 안 나누나».** 나누는 순간 옛 버전으로 열면 목록이 비어 보입니다.
> 대부분의 사람은 색인이 몇십 KB 라 나눌 이유가 없습니다. **필요한 사람만 넘어가게** 했습니다.
> 한 번 나눈 뒤에는 되돌리지 않습니다 (오르내리며 파일을 만들었다 지웠다 하지 않게).

> 💡 **왜 조각 수를 «두 배씩».** `hash % n` 이라 n 이 바뀌면 전부 다시 짜야 합니다.
> 두 배씩 늘리면 그 일이 아주 가끔만 일어납니다. 그때는 조각을 전부 다시 올립니다.

## 함께 고친 것 — `localStorage` 가 넘칠 때의 거짓말

같은 규모에서 걸리는 **두 번째 벽**입니다. 기록이 수천 편이면 브라우저가 내주는 자리(5MB 남짓)를
넘어섭니다. 그때 전에는 「드라이브에 연결하면 해결됩니다」 라고 했는데 **사실이 아닙니다.**
연결해도 이 기기의 자리는 그대로입니다.

이제는 사실대로 말합니다 — 「이 기기에 다 담지 못했습니다. 드라이브에는 그대로 있습니다 ·
새로고침하면 드라이브에서 다시 받아옵니다.」 실제로 그렇게 동작합니다.

> ⚠️ **이 벽 자체는 아직 안 치웠습니다.** 진짜로 치우려면 기록을 `localStorage` 말고
> IndexedDB 로 옮겨야 합니다. 십삼 년치를 실제로 넣어 보고 **정말 넘치는지 먼저 보세요.**
> 넘치기 전에 옮기는 것은 큰 공사만 됩니다.

## 점검

`node docs/점검도구/색인쪼개기.mjs` — **22가지.**
«올린 것을 실제로 들고 있는» 가짜 드라이브를 세워, 올렸다 → 새로고침했다 → 다시 읽었다 를
한 줄로 겪게 합니다. 앞선 점검 도구들의 가짜 드라이브는 올린 것을 버렸습니다.

보는 것 여덟 —
① 작을 때는 안 나눈다 · ② 커지면 나뉜다 · ③ **고친 것이 든 조각 하나만** 올린다 ·
④ 빈 기기에서도 400편이 다 돌아온다 · ⑤ **조각을 못 읽으면 한 글자도 안 덮어쓴다** ·
⑥ 옛 형식으로 덮여도 조각을 찾아 합친다 · ⑦ 조각을 다시 나눠도 800편이 안 빠진다 ·
⑧ 지운 것은 조각에서도 빠지고 되살아나지 않는다

> ⚠️ **`saveIndex` · `loadIndex` 를 건드리면 이 점검을 꼭 돌리세요.**
> 「조각을 못 읽으면 한 글자도 덮어쓰지 않는다」 줄이 빨개지면 **그대로 배포하지 마세요.**

기존 점검도 다 다시 돌렸습니다 — 화면점검 79 · 자료가져오기 55 · 구석구석 27 · 소개페이지 27 ·
공유쪽 16 · 이름바꾸기 11 · 대비점검 28조합 「걸리는 것 없음」.

## 진짜 계정에서 봐야 할 것 (여기서는 못 본 것)

1. **조각이 실제로 생기는지** — 가짜 드라이브로만 봤습니다. 십삼 년치를 넣으면
   폴더에 `TRACE-index-00.json` … 이 생겨야 합니다
2. **그 뒤로도 저장이 빠른지** — 이게 이 작업을 한 이유입니다. 조각이 생긴 뒤
   기록 하나를 고쳐 보고 «저장 중» 이 금방 끝나는지 보세요
3. **폰에서** — 손해가 가장 큰 곳이 폰입니다

## 하다가 발견한 것 (v6 밖)

- **`대비점검.mjs` 는 첫 인자가 «그림 넣을 폴더»** 입니다. 안 주면 저장소 뿌리에 PNG 14장을 흘립니다.
  커밋하기 전에 `git status` 로 한 번 보세요
- **점검이 줄줄이 이상하게 죽으면, 도구를 의심하기 전에 «띄우기 가 아직 살아 있는지»부터 보세요.**
  v6 에서 여기에 한 번 속았습니다 — 아래 함정 모음에 적었습니다

---

# ▶▶ 2026-08-21~22 에서 한 일 — 자료 끌어오기 · 모양 프리셋 · 웹 쪽 공유

커밋 셋입니다.

| 커밋 | 한 일 |
|---|---|
| `f5166de` | 폴더에서 **글 말고 자료까지** 끌어오고, 그 원본을 지켜 준다 |
| `ffd924d` | **모양 프리셋 일곱** · **웹 쪽으로 공유(view.html)** · 녹음 제목 분리 · 폴더 찾아보기 · 목록=디렉토리 |
| `8841674` | 프리셋 색이 **안 읽히던 열두 곳**을 고치고, 그걸 재는 도구를 넣는다 |
| `e71aa55` | 인수인계서를 지금 상태에 맞춘다 |
| `cf391cb` | **공유하는 길 넷을 이름으로 갈라 놓는다** (①②③④ · 고르는 잣대) |

# ▶▶ v5 이어 작업 (2026-08-21) — 십삼 년치를 실제로 끌어올 수 있게

**「이 도구를 만든 진짜 목적」이라고 적어 두었던 것을 열었습니다.**
그전까지 📥 폴더 훑어서 가져오기는 **`.md` 만** 색인에 넣었습니다.
그런데 십삼 년 동안 쌓인 것은 대부분 글이 아니라 **자료**입니다 —
한글 학습지, 발표 자료, 단원평가, 수업 사진, 연수 PDF. 그게 하나도 못 들어왔습니다.

| 무엇 | 전 | 후 |
|---|---|---|
| 색인에 들어가는 것 | `.md` 만 | **글(`.md`) + 자료 전부** — 한글·발표·표·PDF·사진·영상·소리·묶음·구글 문서 |
| 파고드는 깊이 | 5단계 | **10단계** (`SCAN_DEPTH`) |
| 훑는 폴더 수 | 400곳 | **3000곳** (`SCAN_FOLDER_MAX`) |
| 한 폴더에 1000개 넘을 때 | **1001번째부터 조용히 없는 것이 됨** | 쪽을 넘겨 **다 받는다** (`nextPageToken`) |
| 한 번에 넣는 자료 수 | — | **3000개** (`IMPORT_MAX`). 넘치면 최근에 고친 것부터 |

### 어떻게 세우는가

자료 파일 하나가 **기록 한 편**이 됩니다. **원본은 옮기지도 고치지도 않습니다.**
색인이 그 파일을 **가리키기만** 합니다.

- 사진 → `image` 블록 (목록에서 바로 보인다) · 그 밖 → `file` 블록 (첨부 줄)
- **구글 문서·시트·슬라이드 → `link` 블록.** 내려받을 실체가 없어서 링크로 겁니다
- **거쳐 온 폴더 이름이 태그가 됩니다** — `2019/3학년/과학/` 이면 `#2019 #3학년 #과학`
  (위로 3겹까지, `IMPORT_TAG_DEPTH`). 그래서 「태그가 곧 폴더」와 관계망에 그대로 들어옵니다
- 유형은 **「자료」**. 없으면 맨 앞 유형

### 말없이 수천 개를 넣지 않습니다

십삼 년치는 수천 개가 나옵니다. 그래서 넣기 전에 **무엇이 몇 개인지 보여 주고 물어봅니다** —
「사진 1,204개 · 한글 312개 · PDF 88개…」. 글(`.md`)은 물어보기 **전에** 먼저 읽습니다.
그래야 「그 글에 딸린 사진」이 자료 수에서 빠져서, 보여 주는 숫자가 정직해집니다.

> `confirmModal` 에 **`okLabel`·`onCancel`** 을 붙였습니다.
> ⚠️ `onCancel` 은 취소·바깥 누름 **어느 쪽이든 꼭 한 번** 불려야 합니다.
> 안 불러 주면 기다리던 약속(Promise)이 **영영 안 풀려** 가져오기가 멈춘 채로 끝납니다.

### ⚠️ 여기서 두 번 샜습니다 — 다음 사람은 밟지 마세요

둘 다 **「또 눌렀을 때 목록이 불어나는」** 결함입니다. 한 번 눌러 보는 것만으로는 안 보입니다.
`자료가져오기.mjs` 가 **두 번 눌러 보기** 때문에 잡혔습니다.

| 샌 곳 | 왜 | 어떻게 막았나 |
|---|---|---|
| **`.md` 바로가기** | 「태그가 곧 폴더」가 태그 폴더마다 `.md` **바로가기**를 놓는다. 이름이 진짜 글과 **똑같다.** 그래서 매번 «새 글» 로 알고 읽으려 든다. 읽으면 본문이 비어 있어 끝없이 「글 N편을 읽는 중」 만 뜬다 | 글을 고를 때 `mimeType !== "…google-apps.shortcut"` 을 함께 본다 |
| **구글 문서** | 링크 블록이라 **`fileId` 가 안 남는다.** 「이미 들어온 것」을 블록의 `fileId` 로만 알아봤기 때문에, 누를 때마다 «처음 보는 것» 이 됐다 | 기록에 **`srcId`**(원본 파일 ID)를 남긴다. **`slim()` 에도 넣어야** 저장을 견딘다 |

> 📌 **`srcId` 는 `slim()` 을 반드시 함께 고쳐야 합니다.** `slim()` 은 적어 둔 항목만 남기고
> 나머지를 버립니다. 거기 빠뜨리면 **새로고침 한 번에 사라지고**, 그다음부터 또 불어납니다.

### 🎨 모양 프리셋 — 일곱 갈래를 처음에 고른다

일곱 갈래를 **「아이답게 → 정밀하게」 한 축** 위에 놓았다. `THEMES` 의 **목록 순서가 곧 그 축**이고,
고르는 화면에도 그 축을 한 줄로 그려 준다 (`themeAxis()`).

| 차례 | 갈래 | 결 | 글꼴 |
|---|---|---|---|
| 1 | **블록놀이** | 굵은 테두리 3.5px · 큰 돌기 · 원색 · 딱딱한 그림자 | Jua |
| 2 | **메모지** | 손그림 삐뚤 테두리 · 파스텔 쪽지 | Gaegu |
| 3 | **블록** | 차분한 브릭 · 돌기 | Gaegu |
| 4 | **공책** | 속지 · 왼쪽 색줄 | Gaegu |
| 5 | **기본** | 지금 그대로 (중립) | 기기 글꼴 |
| 6 | **문서** | 명조 본문 · 모서리 2px · 무채색 | Gowun Batang |
| 7 | **설계도** | 모서리 0 · 머리카락 선 · 차가운 회청색 · 빽빽하게 | IBM Plex Sans KR |

> 글꼴은 `THEMES[].font` 에 적어 두고 **고른 것만 받아 온다** (`ensureThemeFont`).
> 고르는 화면에서만 일곱을 다 받는다 (`ensureAllThemeFonts`) — 한눈에 견줘야 하기 때문이다.

처음 오는 사람에게 **묻고**, 이미 쓰던 사람에게는 **띠로 알린다**
(쌓아 둔 기록이 있는 사람 앞을 창으로 막아서면 남의 일을 가로채는 것이다).
나중에는 `⚙️ 설정 → 모양` 에서 언제든 바꾼다. 고른 값은 `settings.theme` 에 들어가 **계정을 따라간다.**

> 📌 **`""`(빈 값)과 `"base"` 는 다르다.** 빈 값은 «아직 안 골랐다», `base` 는 «기본을 골랐다».
> 같게 두면 기본을 고른 사람에게 매번 다시 묻는다.

#### 🎨 색을 손대면 반드시 `대비점검.mjs` 를 돌리세요

프리셋 일곱 × 밝은/어두운 × 데스크톱/폰 = **스물여덟 자리**를 수치로 잽니다.
눈으로는 못 잡습니다 — **「블록놀이」의 «아이디어» 배지가 3.15:1** 이었습니다.
하필 아이가 볼 화면인데, 화면으로는 그냥 «노란 배지» 로 보여서 못 알아챘습니다.

```bash
node docs/점검도구/대비점검.mjs <그림 넣을 폴더> http://localhost:8000/
```

마지막 점검에서 열두 곳을 기준(4.5:1)에 맞췄습니다 — 색조는 그대로 두고 밝기만 옮겼습니다.

| 고친 곳 | 전 → 후 |
|---|---|
| 블록놀이 유형1·3·4·5 | 4.12 · **3.15** · 3.45 · 4.13 → 4.77 · 4.63 · 4.75 · 4.75 |
| 설계도 유형1·3·4 | 4.29 · 4.23 · 4.24 → 4.71 · 4.71 · 4.80 |
| 메모지·블록·공책 `--ink-soft` | 4.39 → 4.75 (밝은 화면) |
| 블록놀이 `--ink-soft` | 4.48 → 4.68 |
| 종이 계열 어두운 화면 `--ink-soft` | 4.32 → 4.75 |

> 🕳️ **잣대를 짤 때도 한 번 빠졌습니다.** 배경 «색» 만 보면 안 됩니다.
> 기록 카드는 유형 색을 색으로 깔고 그 위에 덮개(`--veil`)를 배경 «그림» 으로 칠합니다.
> 색만 보면 가려진 틴트를 배경으로 잡아, **멀쩡한 것을 «미달» 이라고** 말합니다.
> 처음 재었을 때 「기본」이 미달로 나온 게 그 탓이었습니다.

#### ⚠️ 다음 사람이 반드시 지켜야 할 한 가지

**프리셋마다 화면 CSS 를 따로 쓰지 마세요.** 그러면 기능을 하나 고칠 때마다 네 벌을 고쳐야 하고,
한 벌을 빼먹는 순간 그 프리셋만 깨집니다. 규칙은 이렇습니다.

- 화면 CSS 는 **하나뿐**이다. 프리셋은 `:root` 의 **토큰 값만** 갈아 끼운다
- 새 화면을 만들 때 색·테두리 굵기·모서리를 **숫자로 쓰지 말고 토큰으로** 쓴다
  (`var(--bw)` · `var(--r-btn)` · `var(--radius)` · `var(--font-title)` …)
- 그러면 **새 기능이 네 프리셋에 저절로 따라온다.** 프리셋을 손댈 일이 없다

| 토큰 | 무엇 |
|---|---|
| `--font-body` · `--font-title` · `--title-spacing` | 글꼴. 손글씨(Gaegu)는 **제목·단추에만** |
| `--bw` | 테두리 굵기 (1px ↔ 2.5px) |
| `--radius` · `--r-btn` · `--r-btn-sm` · `--r-inp` · `--r-pill` · `--r-block` | 모서리. 메모지는 여기에 **삐뚤한 값**을 넣어 손그림처럼 만든다 |
| `--veil` | 기록 카드의 **종이색 덮개**. `transparent` 로 바꾸면 유형 색이 드러난다 |
| `--entry-pt` · `--accent-w` · `--accent-l` | 카드 위 여백 · 위 색줄 · 왼 색줄 |
| `--stud` · `--stud-w` · `--stud-h` · `--stud-step` | 레고 돌기 — 켜기 · 줄 너비 · 지름 · 간격 |
| `--warn-bg/line/ink` · `--ok-…` · `--pin-bg` | 알림 띠와 고정 딱지. 뜻은 그대로 두고 **색만** 프리셋이 고른다 |
| `--t1`~`--t6` · `--t1s`~`--t6s` | **유형 색 여섯 자리** (진한 색 / 연한 색) |

#### ⚠️ 유형 색은 «키» 가 아니라 «차례» 로 준다

사용자가 유형을 더 만들 수 있습니다. 색을 `experience` 같은 **키로 정하면 사용자가 만든 유형에는
색이 없어집니다.** 그래서 `typeVars()` 가 유형의 **자리(index)** 를 여섯으로 나눠 돌려 씁니다.

#### 🕳️ 여기서 한 번 빠졌습니다 — `var()` 는 «선언한 곳» 에서 풀린다

`--entry-bg: var(--c-soft, var(--panel))` 이라고 써 놓고 카드마다 `--c-soft` 를 달았는데,
**카드는 제 색을 못 입었습니다.** 안쪽 `var()` 는 그 토큰을 **선언한 곳(html)** 에서 풀리기 때문입니다.
거기엔 `--c-soft` 가 없으니 언제나 대비값(`--panel`)이 됐습니다.

고친 방법 — **덮개를 걷는 방식**으로 뒤집었습니다.
카드 바탕색은 언제나 유형 색이고(`background-color: var(--c-soft, …)` — 이건 **카드에서** 풀린다),
그 위에 `--veil` 색을 한 겹 덮습니다. 기본·공책은 종이색으로 덮어 가리고, 메모지·블록은 `transparent` 로 걷습니다.

### 🎙️ 녹음 — 제목과 내용을 갈라 놓았다

전에는 받아 적은 글의 **앞 60자가 «자료 제목»** 이 되었다. 자료 제목은 파일 이름에 그대로 들어가므로,
말한 문장이 통째로 파일명이 됐다.

    2026-08-21_부기_협의회_아직 교사가 안 다듬어져서 요거 다듬어지고 나면 이제 학생은…_02.webm

녹음 창에 **«제목» 칸을 따로** 두고, 받아 적은 글은 아래 글 블록만 맡는다.
비워 두면 날짜·시각으로 저장된다.

> ⚠️ 녹음 창은 시계 때문에 **가운데 정렬**이다. 적는 칸을 새로 넣을 때는
> `style.textAlign = "left"` 를 꼭 돌려 놓아야 라벨이 가운데로 가지 않는다.

### 🗂️ 폴더 정리 방식 — 고르는 목록을 걷어냈다

`<select>` 였다. **펼치면 그 아래 폴더 그림을 통째로 덮었다** — 정작 봐야 할 것을 가리는 셈이었다.
다섯 갈래를 **각자 제 폴더 그림을 달고** 나란히 펼쳐 놓는다 (`.modecard` · `folderTree()`).

- 폴더·글·사진·바로가기 아이콘은 **그려서** 쓴다 (`fileIconSvg`). 그림글자는 기기마다 모양이 달라진다
- 그림 옆 화살표 설명은 **아래 한 줄과 겹치는 말만 빼고** 남겼다 — 안 그러면 잘리기만 한다

### 📂 폴더 찾아보기 — 탐색기처럼

링크를 붙여넣는 길은 «주소를 아는 사람» 에게만 쉽다. `openFolderBrowser()` 가 내 드라이브를 열어
눌러 들어가며 고르게 한다. 안에서 **새 폴더**도 만든다.

> ⚠️ **「내 드라이브」 통째로는 못 고르게 막았다.** 뿌리에 연결하면 십삼 년치가 든 곳에
> 기록장 파일이 흩뿌려진다.
> ⚠️ 물음에 «폴더만» 을 걸어 두었지만 **받은 것도 한 번 더 거른다.** 파일이 섞여 나오면
> «눌러도 안 열리는 줄» 이 생겨 고장 난 것처럼 보인다.

### 🌐 «웹 쪽으로» 공유 — 사진·영상·녹음까지 (view.html)

사용자가 «HTML 로, 사진·영상·녹음이 잘 보이게» 를 원했다. 앞의 두 길로는 안 된다.

| 길 | 왜 모자란가 |
|---|---|
| `.html` 파일을 드라이브에 올리기 | **드라이브가 웹페이지로 안 연다** (2016년에 정적 호스팅을 없앴다). 내려받기 화면만 뜬다 |
| 구글 문서 «웹에 게시»(`/pub`) | 사진은 실리지만 **영상·녹음은 못 싣는다** |

그래서 **읽는 쪽 `view.html` 을 이 저장소에 두었다.** 저장소 전체가 Pages 로 배포되니 새 서버가 필요 없다.

- **글·제목·태그·고른 모양**은 **주소 뒤(`#d=`)** 에 gzip 으로 눌러 담아 보낸다 → 서버도, 새 열쇠(API 키)도 필요 없다
- **사진·영상·녹음**은 드라이브에서 바로 불러온다 (`<img>` · `<iframe>` 은 CORS 를 안 탄다)
- 실제로 재 보니 꽤 긴 기록도 **700~800자** 였다. 카톡에 넣을 만하다

```
보내는 쪽 index.html : buildSharePacket → encodePacket → view.html#d=...
받는 쪽  view.html   : decodePacket → render
```

> 📌 **`encodePacket`(index.html)과 `decodePacket`(view.html)은 «짝»이다.**
> 맨 앞 한 글자가 표시 — `1` 은 눌러 담은 것, `0` 은 그냥 담은 것.
> 한쪽만 고치면 링크가 통째로 안 펴진다. 꾸러미 모양(`{v,t,d,g,th,b}`)도 마찬가지다.

#### 드라이브에서 남에게 보여 주는 주소 (문서에 없는 길이다)

| 무엇 | 주소 |
|---|---|
| 사진 | `lh3.googleusercontent.com/d/{id}=w1600` → 막히면 `drive.google.com/thumbnail?id=…` → 그것도 막히면 **«열어 보기» 링크로 내려간다** |
| 영상·녹음 | `drive.google.com/file/d/{id}/preview` 를 `<iframe>` 으로 |

> ⚠️ **구글이 문서로 약속한 주소가 아니다.** 언젠가 바뀔 수 있어서 사진에는 두 겹의 대비책을 뒀다.
> 대비책이 없으면 받는 사람 화면에 **깨진 그림만** 남는다.

> ⚠️ **녹음 칸을 62px 로 두면 안 된다.** 우리 재생기가 아니라 «드라이브가 그리는 쪽» 이라
> 잘려서 「Google Drive」 글자만 보인다. 168px 준다.

> ⚠️ **주소의 `#` 만 바뀌면 브라우저는 쪽을 다시 읽지 않는다.** `hashchange` 를 듣지 않으면
> 링크 두 개를 이어서 열었을 때 앞의 기록이 그대로 남는다.

> ⚠️ **`CompressionStream` 의 `write()`·`close()` 가 돌려주는 약속을 그냥 두면 안 된다.**
> 링크가 잘려 풀다 막히면 «아무도 안 받은 거부» 가 되어 콘솔에 오류로 남는다. 양쪽 다 `.catch` 로 삼킨다.

> ⚠️ **이 길은 딸린 파일을 하나씩 «링크가 있는 사람 = 읽기» 로 엽니다.** 그래서 공유 창의
> **사진 확인 관문**을 반드시 지나야 한다. «공유 끄기» 는 그 파일들의 권한을 도로 거둔다.

**점검**: `node docs/점검도구/공유쪽.mjs` — 16가지. 진짜 `view.html` 이 펴는 것까지 본다.
다만 **사진·영상이 실제로 보이는지는 실제 계정이라야 안다** (여기서는 가짜 ID라 대비책으로 내려간다).

### 🌐 웹 페이지로 공유 — 드라이브는 HTML 을 못 연다

**`.html` 을 드라이브에 올려 링크를 줘도 «웹페이지» 로 안 열린다.**
구글이 2016년에 드라이브의 정적 웹 호스팅을 없앴다 — 내려받기 화면만 뜬다.

대신 **구글 문서를 «웹에 게시»** 한다 (`publishDocToWeb`). 문서의 판(revision)에 `published` 를 켜는 것이라
편집 화면이 아니라 **읽는 쪽(`/pub`)** 이 열린다. 사진·링크·소제목이 그대로 있고, 받는 사람은 로그인이 필요 없다.
`publishAuto` 를 켜야 나중에 고친 것이 게시된 쪽에도 따라간다.

### 🔲 모자이크 칸 굵기를 고를 수 있게

칸 크기가 `r / 4` 로 고정이라 원 하나에 여덟 칸뿐이었다 — 굵을 수밖에 없었다.
`GRAINS` 로 **곱게 · 보통 · 굵게** 를 두고 기본을 «보통»(`r / 7`)으로 내렸다. `settings.maskGrain` 에 남는다.

> 곱게 갈수록 보기엔 곱지만 **원래 얼굴이 되살아 보인다.** 그래서 사람이 고르게 두었다.

### 🗃️ 목록 보기 = 디렉토리 · 연결을 그 자리에서

목록 보기가 **지금 정리 방식대로 폴더에 담겨** 나온다 (`listFolderOf()`). 드라이브를 열었을 때와 같은 모양이라
화면과 드라이브가 어긋나지 않는다. 폴더 줄을 눌러 접었다 편다.

🔗 를 누르면 **이어진 기록이 그 자리에서 펼쳐진다** (`linkOpen`). 잇거나 끊는 것은 여전히 편집·관계망에서 한다.

> ⚠️ `outLinks` 는 `{ to, label }`, `backLinks` 는 **`{ entry, label }`** 이다. 생김새가 다르다.
> 한 가지로 뭉뚱그리면 «들어오는 연결» 이 통째로 안 보인다. 한 번 그렇게 짰다가 고쳤다.
> ⚠️ 폴더 이름이 날짜로 시작하므로 **내림차순** 이라야 최신이 위로 온다.

### 🔄 「폴더 구조」 창은 드라이브를 읽는 것이 아니었다

사용자가 드라이브에서 **손으로 만든 폴더가 안 나온다**고 했다. 맞다 — 이 창은 색인으로 그린
**«이렇게 됩니다» 예상도**였다. 아무리 다시 열어도 안 나온다.

- 그 사실을 **화면에 적어 두었다.** 말해 주지 않으면 «고장 났다» 고 여긴다
- **「🔄 드라이브에서 다시 읽기」** 를 넣었다 (`buildLiveTree`). 진짜로 훑어 지금 있는 그대로 그린다
- ⚠️ `scanFolder` 가 **지나간 폴더도 적어 두게**(`dirs`) 고쳤다. 파일만 모으면 **빈 폴더가 통째로 사라진다** —
  손으로 만들어 아직 아무것도 안 넣은 폴더가 딱 그 경우다

---

### 🧭 길찾기 줄 — 이 기록이 «어디에» 있는지

    내 폴더 › 2019 › 3학년 › 과학

폴더를 훑을 때 거쳐 온 길을 **`srcPath`** 에 통째로 남깁니다 (태그는 뒤 3칸만 쓰지만, 줄에는 전부 필요합니다).
칸을 누르면 **그 아래에 있는 기록만** 남고(`filterPath`), 위쪽에 `📁 2019 / 3학년 아래` 칩이 떠서
지금 어디를 보고 있는지 알려 줍니다. 앞에서부터 같은 것만 남기므로 `2019/3학년` 을 고르면 `2019/4학년` 은 빠집니다.

> `srcPath` 도 **`slim()` 에 넣어야** 새로고침을 견딥니다. `srcId`·`srcFolderId` 와 같은 자리입니다.

---

### 🚨 가장 무거웠던 것 — 지웠더니 십삼 년치가 통째로 갔다

**가져오는 것보다 «가져온 뒤» 가 훨씬 위험했습니다.** 이건 v5 부터 있던 결함인데,
그때는 `.md` 몇 개만 들어와서 안 보였습니다. 자료 수천 개가 들어오면서 드러났습니다.

이 앱에서 **`folderId` 는 「이 기록이 가진 폴더」** 라는 뜻입니다. 코드 네 군데가
그 뜻을 믿고 움직입니다. 그런데 가져온 기록에 **원본이 있던 남의 폴더**를 `folderId` 로
넣어 두었더니, 그 네 군데가 전부 **십삼 년치가 든 폴더**를 향했습니다.

| 어디 | 무슨 일이 벌어졌나 |
|---|---|
| `removeEntry` | 학습지 **한 편** 지우면 `trashFile(n.folderId)` → **`2019/3학년/과학/` 이 통째로 휴지통행** |
| `openShareModal` 의 «📁 폴더 통째로» | 자료 하나 공유하려다 **그 폴더 전체가 «링크 있는 사람 = 읽기»** 가 됨 |
| `relocateEntryFiles` | 한 번 고쳐 저장하면 원본이 **십삼 년 있던 자리에서 새 폴더로 끌려 나옴** |
| `saveEntry` | 첨부 줄을 빼고 저장하면 `trashFile(b.fileId)` → **원본 파일이 휴지통행** |

**고친 방법 — 이름을 갈라 놓았습니다.**

- 가져온 기록은 **`folderId` 를 갖지 않습니다.** 대신 **`srcFolderId`·`srcFolderName`**
  («원본이 있던 곳») 에 적고, **길 안내에만** 씁니다 — 화면에 「📁 **원본** 폴더」로 뜹니다
- `removeEntry` 는 `srcId` 가 있으면 **아무것도 안 버리고 목록에서만 뺍니다.**
  확인 창 문구도 「목록에서만 뺍니다 … 원본 파일은 그대로 남습니다」로 갈라 씁니다
- `relocateEntryFiles`·`saveEntry` 는 **`fileId === n.srcId` 인 것을 건너뜁니다**

> 📌 **다음에 `folderId` 를 쓸 일이 생기면 먼저 물어보세요 — 「이 폴더는 이 기록이 만든 것인가?」**
> 아니라면 `folderId` 에 넣으면 안 됩니다. 이름이 같다고 뜻이 같지 않습니다.

> 📌 **`srcFolderId`·`srcFolderName` 도 `slim()` 에 넣어야 합니다.** 안 그러면
> 새로고침 한 번에 「📁 원본 폴더」가 사라집니다.

### ⚠️ 아직 그대로 둔 것 — 가져온 `.md` 를 고치면 원본이 옮겨집니다

가져온 **`.md`** 는 `srcId` 가 없습니다. 그건 «파일을 가리키는 것» 이 아니라 **기록 그 자체**라서,
앱이 소유권을 갖는 것이 맞다고 보았습니다. 그래서 한 번 고쳐 저장하면 정리 방식에 따라
**그 `.md` 와 딸린 사진이 새 폴더로 옮겨집니다** (점검에서 `MD1`·`IMG1` 이 실제로 옮겨졌습니다).

옵시디언 서고를 통째로 끌어온 사람에게는 **놀랄 일** 일 수 있습니다.
바꾸려면 가져온 `.md` 에도 «제자리에 둔다» 표시를 달아야 하는데,
그러면 v5 의 동작이 바뀌므로 **사용자에게 물어보고 정하세요.**

### 이럴 때는 안 들어옵니다 (일부러)

- `.DS_Store`·`Thumbs.db`·`~$…`·`.tmp`·`.lnk` 같은 **부스러기**
- **바로가기**(`google-apps.shortcut`) — 안 거르면 태그 수만큼 같은 기록이 늘어납니다
- **글에 딸린 사진·첨부** — 이미 그 글의 일부입니다

### 확인한 것 / 못 한 것

`자료가져오기.mjs` **52가지 통과** — 가져오는 것 25가지 + 가져온 뒤(고치기·빼기·지우기·공유) 15가지 — 로그인 없이 «가짜 드라이브» 를 세워서 봅니다.
기존 점검도 그대로입니다 (화면점검 66 · 구석구석 26 · 이름바꾸기 11).

**못 한 것: 진짜 십삼 년치 폴더.** 아래 「실제 기기로 확인하지 못한 것」 6번을 보세요.

---

# ▶▶ v5 에서 한 일 (2026-08-20 ~ 08-21) — 커밋 24개

사용자가 「업그레이드」로 적어 온 목록을 그대로 만들고, 이름을 두 번 바꾸고,
소개 페이지를 새로 만들었습니다.
**AI 가 필요한 것(손글씨 자동 인식·녹음 자동 요약)은 사용자가 「빼자」고 해서 넣지 않았습니다.**

| 커밋 | 한 일 |
|---|---|
| `9afd262` | 로그인 풀림 · 업로드 멈춤 · 사진 모자이크 · 링크 공유 |
| `161e3f0` | 웹 캡처 (폰 공유 시트 + PC 즐겨찾기) |
| `a81bfd4` `44373be` | 손 메모 |
| `779e801` | 녹음 · 받아쓰기 |
| `7a1fa74` | 태그가 곧 폴더 · 폴더 지도 |
| `5852749` | 폴더 지도 → **접었다 펴는 폴더 트리**로 교체 (사용자 요청) |
| `36f940c` | **폴더 통째로 공유** + **공유 전 사진 확인 관문** |
| `594c1eb` | **원 도장으로 여러 장 한 번에 가리기** + 올라간 사진 덮어쓰기(`_remask`) |
| `fb0abda` | 폰에서 머리줄 단추가 화면 밖으로 밀려나던 것 (진짜 결함) |
| `4a14522` | **소개 페이지** `intro.html` + 캡처 10장 |
| `cb4f62a` `cc4d449` | PKEMS → **PEER** (저장소·폴더까지) |
| `c4524be` | PEER → **TRACE**, 도구 이름은 「구글 기록장」으로 되돌림 |
| `4bac1e7` | 파일 이름에 **{태그}·{자료제목}** · 📛 **밖에서 넣은 파일 이름 정리** |
| `9d31978` | 소개 페이지를 **쪽으로 갈린 페이지**로 (스크롤 대신 탭) |
| `c1adbed` `b00ca67` | 소개 글을 만든이의 말로 다시 씀 · 탭 이름 정리 |

### 얼굴 자동 모자이크를 왜 안 넣었나

사용자가 물었고, **못 한다가 아니라 「넣으면 더 위험하다」** 가 답이라 적어 둡니다.

- `window.FaceDetector`(Shape Detection API)는 **엣지·크롬 데스크톱에 없습니다.**
  실제로 확인했습니다 (`typeof window.FaceDetector === "undefined"`).
  안드로이드 일부에만 있고 그마저 플래그 뒤에 있습니다
- 외부 모델(TensorFlow.js BlazeFace 등)을 붙이면 되긴 합니다. 다만
  ① 2MB 짜리 모델을 받아야 하고 ② 「파일 하나·라이브러리 없음」 원칙이 깨지고
  ③ **가장 큰 문제 — 놓친 얼굴은 그대로 공개됩니다.** 옆모습·뒷모습·가려진 얼굴·작은 얼굴은
  자주 놓칩니다. 「자동으로 가려 준다」를 믿게 만들어 놓고 놓치는 것이,
  아무것도 안 해서 사람이 직접 보는 것보다 나쁩니다
- 그래서 **공유 전 사진 확인 관문**을 대신 넣었습니다. 실패해도 «못 가림» 이지
  «가린 줄 알았는데 안 가림» 이 아닙니다

사용자가 그래도 원하면 그때 라이브러리를 넣으면 됩니다. 다만 **「보조일 뿐, 최종 확인은 사람」**
이라는 문구를 반드시 함께 붙여야 합니다.

**대신 사용자가 낸 안을 만들었습니다** — 「수동이지만 한 번에, 원 도장으로」.
`openPhotoMaskStudio()`. 이 기록의 사진을 한 줄로 늘어놓고, 얼굴 위를 톡 누르면
동그랗게 뭉갭니다. 쓸면 줄줄이 찍힙니다. 크기 4단계, 누르기 전 동그라미 미리보기.

- 찍은 자리를 **`stamps[]` 목록으로만** 들고 있다가 매번 원본에서 다시 그린다.
  되돌리기가 공짜고 여러 번 찍어도 화질이 안 상한다
- **드래그 중에는 전체를 다시 그리지 않는다.** 방금 찍은 원 하나만 덧그린다.
  2400px 사진에서 매번 다시 그리면 손가락을 못 따라온다
- 도장 간격은 **반지름의 절반** — 쓸 때 도장이 수백 개 쌓이는 것을 막는다
- ⚠️ `pixelateCircle` 에서 원이 사진 왼쪽·위로 넘칠 때 **네모의 시작점만 0 으로 밀면 안 된다.**
  엉뚱한 자리의 그림을 떠 오게 된다 (가려지긴 하는데 엉뚱한 무늬가 붙는다).
  `x2 = min(width, cx+r)` 로 **끝점까지 같이 잘라야** 한다. 한 번 그렇게 짰다가 고쳤다

### 올라간 사진을 고쳐 다시 올리는 길 (`_remask`)

가리기는 **이미 드라이브에 있는 사진에도** 된다. 새 파일을 만들지 않고
**같은 `fileId` 에 덮어쓴다** (`uploadBlob(..., fileId)` = PATCH). 파일 이름·주소가 그대로라
색인도 글 속 링크도 안 어긋난다.

블록에 `_remask = true` 를 달아 두면 `syncPending` 이 그 사진만 다시 올린다.
`_remask` 는 세 곳에서 함께 봐야 한다 — **하나라도 빠뜨리면 새로고침에 가린 것이 날아간다.**

| 곳 | 하는 일 |
|---|---|
| `stashPending()` | 본문을 IndexedDB 에 넣는다 |
| `rehydratePending()` | 새로고침 뒤 되살린다 |
| `restoreDraft()` 의 idbAll | 쓰다 만 것도 되살린다 |
| `syncPending()` todo 거르기 + 덮어쓰기 단계 | 실제로 다시 올린다 |

> 드라이브 **버전 기록에는 가리기 전 사진이 남습니다.** 내 계정에서만 보이고
> 링크로 받은 사람은 못 봅니다. README 의 「한계」에 적어 두었습니다.

## ⚠️ v5 에서 알아낸 것 — 다음 사람이 꼭 알아야 할 두 가지

### 1. 구글 토큰 창은 «조용히» 열 수 없다

`google.accounts.oauth2` 에는 **«조용히 받기» 라는 길이 없습니다.** 부르면 **무조건 팝업**입니다.
사용자가 아무것도 안 누른 상태(예: 페이지 로드 직후)에서 부르면 브라우저가
「내가 안 시킨 창」으로 보고 **막습니다.** 그리고 예전 코드는 그 실패를
`.catch(function(){})` 로 삼켰습니다 — **이것이 「폰에서 로그인이 풀린다」의 진짜 정체입니다.**

지금은 이렇게 합니다.
- 토큰을 **만료시각과 함께 `localStorage`** 에 둔다 (`trace.token.v1`). 살아 있으면 그대로 쓴다
- `canOpenPopup()` — **최근 60초 안에 누른 적이 있을 때만** 토큰 창을 연다
- 못 열면 조용히 넘어가지 않고 `needReconnect = true` → **「다시 연결」 띠**

> 이 사실을 모르고 「startup 에서 조용히 갱신하자」고 고치면 **원래 버그로 되돌아갑니다.**

### 2. `fetch` 로는 업로드 진행률을 못 낸다

`fetch()` 에는 **업로드 진행 이벤트가 아예 없습니다.** 그래서 예전에는 조각(8MB) 하나가
끝나야 진행률이 움직였고, 폰에서 그건 **20~60초 동안 `0%` 에 붙어 있는 것**이었습니다.
사용자가 말한 「멈춘 채로 있어」가 이것이었습니다.

지금은 `XMLHttpRequest` + `xhr.upload.onprogress` 입니다. 되돌리지 마세요.
함께 들어간 것: **45초 정지 감지**(`STALL_MS`) → 끊고 `bytes */total` 로 받은 자리를 물어
이어붙이기, 429·5xx 지수 백오프, 오프라인이면 `online` 이벤트까지 대기,
폰은 조각 2MB, 업로드 중 `wakeLock`.

## 🧪 점검 도구가 생겼습니다 — 고쳤으면 반드시 돌리세요

**이 노트북에 node 가 있습니다** (v24.19.0). 예전 인수인계서에 「node 없음」이라고
적혀 있던 것은 **틀렸습니다.** python(3.12) 도 있습니다.

```bash
powershell -ExecutionPolicy Bypass -File docs/점검도구/띄우기.ps1    # 8000 포트로 띄우기
node docs/점검도구/화면점검.mjs   http://localhost:8000/            # 새 기능      79가지
node docs/점검도구/구석구석.mjs   http://localhost:8000/            # 원래 화면    26가지
node docs/점검도구/이름바꾸기.mjs http://localhost:8000/            # 옛 이름 이어받기 11가지
node docs/점검도구/소개페이지.mjs http://localhost:8000/intro.html <캡처를 넣을 폴더>   # 소개 페이지 27가지
node docs/점검도구/자료가져오기.mjs http://localhost:8000/          # 폴더에서 끌어오기 52가지
node docs/점검도구/공유쪽.mjs     http://localhost:8000/            # 웹 쪽 공유    16가지
node docs/점검도구/대비점검.mjs   <그림 넣을 폴더> http://localhost:8000/  # 프리셋 대비  28조합
```

> ⚠️ **`소개페이지.mjs` 는 두 번째 인자(캡처를 넣을 폴더)가 없으면 죽습니다.**
> 화면을 찍어 그 폴더에 넣기 때문입니다. 빼먹으면 `path … must be of type string` 이 뜹니다.

> 여섯을 다 돌리면 **214가지**입니다. 색을 손댔다면 `대비점검.mjs` 도 함께 돌리세요. 코드를 고쳤으면 **전부** 돌리세요.
> 특히 저장 키를 건드렸다면 `이름바꾸기.mjs` 는 반드시 — 그게 깨지면
> 화면 결함이 아니라 **자료 사고**입니다.

> ⚠️ **점검을 이어 돌릴 때는 앞선 브라우저를 먼저 죽이세요.**
> 도구마다 정해진 포트(9333·9345·9351…)를 씁니다. 앞선 것이 안 죽고 남아 있으면
> 새로 띄운 창이 그 포트를 못 잡고 **죽어 가는 옛 창에 붙습니다.**
> 그러면 「＋버튼 0개」 같은 **헛된 실패**가 납니다. 코드가 깨진 게 아닙니다.
> v5 에서 여기에 한 번 속아 멀쩡한 코드를 의심했습니다.
>
> ```powershell
> Get-CimInstance Win32_Process -Filter "Name='msedge.exe'" |
>   Where-Object { $_.CommandLine -like '*remote-debugging-port=93*' } |
>   ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
> ```

> 🧪 **`자료가져오기.mjs` 는 로그인 없이 «가져오기» 를 끝까지 봅니다.**
> 페이지가 뜨기 전에 `window.fetch` 를 갈아 끼워 **가짜 드라이브**를 앞에 세웁니다
> (`Page.addScriptToEvaluateOnNewDocument`). 폴더 나무·1000개 넘는 폴더의 쪽 나누기·
> 바로가기·부스러기까지 그대로 겪게 만들 수 있습니다.
> **드라이브에 실제로 무엇이 쌓이는지는 여전히 실제 계정이라야 압니다.**

엣지를 화면 없이 띄워 **실제로 눌러 봅니다** — 사진을 넣고, 모자이크를 칠하고, 펜으로 긋고,
녹음을 켜고, 공유를 보냅니다. 그리고 **픽셀 단위로 확인**합니다
(가린 쪽은 바뀌고 안 가린 쪽은 한 점도 안 바뀌었는지 등). 자세한 것은
[docs/점검도구/README.md](점검도구/README.md).

문법만 빠르게 보려면:
```bash
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const i=h.indexOf('<script>');const j=h.lastIndexOf('</script>');fs.writeFileSync('app.js',h.slice(i+8,j))" && node --check app.js
```

> ⚠️ **JScript(`cscript //E:JScript`) 로는 문법 검사가 안 됩니다.** ES3 파서라
> `.catch(...)` 같은 예약어 속성에서 죽습니다. 시도했다가 시간을 버렸습니다.

## ❗ 실제 기기로 확인하지 못한 것 (v5 부터 쌓인 것)

> 📌 **이번에 새로 생긴 것은 맨 앞 「지금 바로 할 일」 에 있습니다.** 여기는 v5 때부터 남아 있던 목록입니다.

점검 도구로는 여기까지가 한계입니다. **실제 폰·실제 계정으로 한 번은 해 봐야 합니다.**

1. **받아쓰기** — 구글 음성인식 서버에 붙어야 해서 화면 없는 브라우저로는 확인 불가.
   **한국어로 한 문단 말해 보고**, 조용할 때 끊겼다 다시 켜지는지 보세요
2. **안드로이드 공유 시트** — 서비스 워커가 POST 를 받는 것까지는 확인했지만,
   **실제 공유 목록에 «기록장» 이 뜨는지**는 폰에 설치해 봐야 압니다
3. **드라이브에 실제로 올라가는 것 전부** — 태그 폴더·바로가기·업로드 이어붙이기.
   로그인이 필요해 자동으로는 못 합니다
3-1. **공유 권한이 실제로 바뀌는지** — 글(구글 문서)과 **폴더** 둘 다.
   `permissions.create {role:"reader", type:"anyone"}` 를 씁니다.
   시크릿 창에서 링크를 열어 **로그아웃 상태에서도 보이는지** 확인하세요.
   그리고 **「공유 끄기」 뒤에는 안 보이는지**까지 봐야 반쪽이 아닙니다
4. **모자이크·손메모·녹음이 드라이브에 파일로 쌓이는지**

5. **📛 밖에서 넣은 파일 이름 정리** — 폴더를 훑어 **실제로 이름을 바꾸는** 기능입니다.
   로그인이 필요해 자동으로는 한 줄도 못 봅니다.
   **자료 몇 개만 든 시험용 폴더로 먼저** 해 보세요. 되돌리려면 손으로 해야 합니다

6. **📥 자료 끌어오기 (v5 이어 작업)** — 가짜 드라이브로는 25가지를 다 통과했지만,
   **진짜 십삼 년치 폴더는 겪어 보지 못했습니다.**
   - **작은 폴더로 먼저** 하세요. 원본을 안 건드리니 잘못 들어와도 목록에서 지우면 됩니다
   - 볼 것: 훑는 데 몇 분이 걸리는지 · 색인(`TRACE-index.json`)이 얼마나 커지는지 ·
     기록이 수천 편일 때 **목록·검색·관계망이 버티는지**
   - ✅ **색인이 커지는 것은 v6 에서 풀었습니다** — 커지면 스스로 조각으로 나뉩니다.
     다만 «조각이 실제로 생기는 것» 은 진짜 계정에서 아직 못 봤습니다

## ✅ 지금 상태 (2026-08-22 기준)

- **푸시·배포 다 끝났습니다.** `legoschool/trace` main 에 올라가 있고,
  `legoschool.github.io/trace/` 와 `.../view.html` 둘 다 200 으로 확인했습니다
- **문서는 다 맞춰 두었습니다** — `README.md` · `사용안내.md` · `docs/점검도구/README.md` · 이 파일
- **v6 에서 색인을 조각으로 나눴습니다** — 아래 「▶▶ v6」. 드라이브 폴더에
  `TRACE-index-00.json` 같은 파일이 보일 수 있고, **그건 지우면 안 되는 것**입니다
- **`SETUP.md` 는 글만 고쳤고 캡처 36장은 그대로입니다.**
  그림 속에 **옛 저장소 이름**이 찍혀 있을 수 있습니다. 다시 찍을 때 함께 맞추세요
- **남은 것은 사람이 눌러 보는 일뿐입니다** — 맨 앞 「지금 바로 할 일」

### 📌 다음에 손댈 만한 것

| 무엇 | 왜 |
|---|---|
| **`SETUP.md` 캡처 다시 찍기** | 저장소·앱 이름이 다 바뀌었는데 그림이 옛것이다. 사본 뜨는 사람이 헷갈린다 |
| **실제 기기 확인 뒤 나온 결함 고치기** | 맨 앞 「지금 바로 할 일」 여섯 가지 |
| **13년치 자료 실제로 끌어오기** | 📥 폴더 훑어서 가져오기 + 📛 파일 이름 정리. 이 도구를 만든 진짜 목적. **v5 이어 작업에서 길은 다 열었다. 이제 실제로 한 번 눌러 보는 일만 남았다** |
| ~~`.md` 만 가져오는 한계~~ | ✅ **v5 이어 작업에서 풀었다.** 한글·발표·표·PDF·사진·영상·구글 문서까지 들어온다 |
| ~~5단계·400폴더 상한~~ | ✅ **v5 이어 작업에서 10단계·3000곳으로 올렸다.** 한 폴더 1000개 넘는 것도 쪽을 넘겨 다 받는다 |
| ~~색인이 커지면 저장이 느려지는 것~~ | ✅ **v6 에서 풀었다.** 256KB 를 넘으면 조각으로 나뉘고 **바뀐 조각만** 올린다 |
| **`localStorage` 가 넘치는 것** | 같은 규모에서 걸리는 **두 번째 벽.** v6 에서 «안내 문구» 만 사실대로 고쳤다. 진짜로 치우려면 기록을 IndexedDB 로 옮겨야 하는데, **정말 넘치는지부터 보고** 정할 일이다 |

---

## 지금 상태 — 한 줄 요약 (v4 까지)

**v4 의 일이었던 「복제 안내 캡처 전면 재작업」은 끝났습니다.**
`setup-01` ~ `setup-36` 을 새로 찍었고, `SETUP.md` 를 그 36장으로 처음부터 끝까지
한 줄로 이어지는 순서로 다시 썼습니다.

**남은 것은 딱 두 장** — 「Google에서 확인하지 않은 앱」과 권한 3줄 허용 화면입니다.
지금은 38번 자리에 **글로만** 적혀 있습니다. 사용자가 「그건 빼고 마무리하자」고 해서 남겨 둔 것이니,
나중에 넣을 때는 아래 「첫 로그인」 항목의 방법대로 하면 됩니다.

> ⛔ **구글 프로젝트를 새로 못 만듭니다.** 「프로젝트 한도에 도달했습니다」가 뜹니다.
> **사용자가 프로젝트를 여러 개 지운 뒤에도 그대로였습니다.**
> 구글은 **지워도 30일 유예 동안 자리를 계속 셉니다.** 화면 안내도 「30일 후에 삭제하도록 예약할 수
> 있습니다」라고 말합니다. 그러니 **오늘 지운 것들은 30일 뒤에야 자리가 빕니다.**
> 사용자 결정: **기존 프로젝트 화면으로 찍고, 설정은 바꾸지 않는다.**
> 「증가 요청」은 계정 명의로 구글에 나가는 요청이라 누르지 않았습니다.

| 항목 | 값 |
|---|---|
| 서비스 주소 | **문서에 적지 않음** (100명 한도 소진 방지). `계정.github.io/저장소이름/` 형태 |
| 원본 저장소 | `legoschool/trace` (Public, 템플릿 저장소) |
| **캡처용 사본** | `legoschool/my-googlenote` (Public, 템플릿에서 생성, **v4 에서 새로 만듦**) |
| 로컬 작업본 | `D:\구글 기록장\trace_구글 기록장\` (git, origin 연결됨) |
| 배포 | `main` 푸시 → GitHub Actions 자동 (1~2분) |
| 구글 프로젝트 | 번호 `255330405723` · 계정 `schoolemotion20@gmail.com` |
| 게시 상태 | **프로덕션 · 외부 · 미검증** · 사용자 한도 **100명 중 1명** |
| 만든이 표기 | **레고학교 미스터리** · 오픈소스 · 적정지식의 범위 안에서 |

---

# ▶ 다음에 할 일

## 1) 구글 클라우드 구간 — 남은 화면

찍은 프로젝트는 **기존 pkems 프로젝트**입니다.
주소는 `console.cloud.google.com/auth/...?project=gen-lang-client-0016801991`
(콘솔 상단 칩에는 **`Gemini API`** 로 보이지만, 브랜딩 앱 이름이 「구글 기록장」이고
승인된 원본에 `legoschool.github.io` 가 들어 있는 **그 프로젝트가 맞습니다**.)

**구글 클라우드 구간은 `setup-15` ~ `setup-31` 로 다 찍었습니다.**

글로만 적고 넘어갈 것 두 가지:

- **「저장」을 두 번 눌러야 한다** — 범위 패널에서 `업데이트` 를 누른 뒤, 본 화면에서 `저장` 을 또 눌러야
  실제로 반영됩니다. 이 프로젝트에서는 저장을 누를 수 없어 그 화면은 못 찍었습니다
- **처음 만든 프로젝트에서는 Drive API 자리에 파란 「사용」 버튼**이 있습니다 (26번은 이미 켜진 모습)

> ⚠️ **이 프로젝트는 실제로 돌아가는 앱의 설정입니다. 저장·업데이트 버튼을 누르지 마세요.**
> 화면만 찍고 나옵니다. 범위 패널은 열어서 찍고 **취소**로 닫으면 됩니다.

## 2) 첫 로그인 — 다 채웠습니다 ✅

`setup-37` ~ `setup-40` 으로 「Google에서 확인하지 않은 앱」·`고급`·이동 링크·권한 확인까지
모두 찍어 넣었습니다. **SETUP.md 에 사진 없는 단계는 이제 없습니다.**

> 처음엔 「이미 권한을 준 계정에는 안 나온다」고 적었는데 **틀렸습니다.**
> 토큰을 새로 받는 흐름에서는 **다시 나옵니다.** 그래서 찍을 수 있었습니다.

### 옛 메모 (참고용)

`schoolemotion20` 계정으로 실제 로그인해서 34~36 을 찍었습니다. 그런데:

> ⚠️ **「확인하지 않은 앱」 화면과 권한 3줄 허용 화면은 못 찍었습니다.**
> 이 계정은 **예전에 이미 이 클라이언트에 권한을 준 상태**라 구글이 두 화면을 건너뛰고
> 팝업이 그냥 닫힙니다. 처음 쓰는 사람에게는 반드시 나오는 화면이라 **안내서에는 있어야 합니다.**
>
> 찍으려면 둘 중 하나가 필요합니다.
> - **권한을 한 번 회수한 뒤** 다시 로그인 — `myaccount.google.com/permissions` 에서
>   해당 앱의 액세스를 삭제하면 다음 로그인 때 다시 나옵니다 (계정 설정을 건드리는 일이라 확인받을 것)
> - **한 번도 로그인한 적 없는 다른 구글 계정**으로 로그인

**「이 폴더로 연결」 뒤의 초록 띠**도 아직입니다. 실제로 연결하려면 드라이브에 폴더를 하나
만들어 그 주소를 넣어야 해서, 거기서 멈췄습니다.

## 3) SETUP.md — 다시 썼습니다 ✅

사용자 말이었던 **“복제해서 가져가는 방법이 너무 부분부분 되어 있어서 전혀 알 수가 없다”** 를
1부~5부 40단계로 끊기지 않게 다시 썼습니다. 사진은 **한 줄에 하나씩** 넣어 크게 보이게 했습니다.

- 1부 저장소 만들기 → 2부 사이트 켜기 → 3부 구글 클라우드 → 4부 열쇠 심기 → 5부 첫 로그인
- 앞에 「필요한 것은 구글 계정과 깃허브 계정뿐」·부별 소요 시간표를 넣었습니다
- 자주 막히는 곳(프로젝트 선택 확인, **저장 두 번**, 원본 주소에 경로 붙이지 말 것,
  Pages 켠 뒤 **Re-run**)에는 ⚠️ 를 달았습니다
- 맨 뒤에 「막혔을 때」 표를 증상별로 넣었습니다

> 기존 `docs/images/01~13` 은 **Fork 기준의 옛 캡처**라 이제 아무도 안 씁니다.
> `D:\구글 기록장\` 경로에서는 `Remove-Item`·`git rm` 이 보호 규칙에 막혀 못 지웁니다. 그냥 두세요.

---

# 캡처 도구 — v4 에서 다시 만들고 **실제로 검증한 조합**

스크래치패드 경로는 세션마다 바뀝니다. 아래 스크립트를 새로 만들어 쓰세요.
**한글 문자열을 스크립트 안에 상수로 넣지 마세요** (인코딩이 깨집니다). 인자로만 넘기세요.

| 스크립트 | 하는 일 |
|---|---|
| `movewin.ps1` | 창을 정해진 크기로 고정 |
| `shotbox.ps1` | 화면을 찍고 붉은 네모·검은 가림 그리기 |
| `click.ps1` | 브라우저 UI(탭·안내 띠)를 진짜 마우스로 클릭 |
| `key.ps1` | 크롬 단축키 보내기 (`^+b` = 북마크바 토글) |
| `boxpng.ps1` | 이미 찍은 png 위에 네모를 다시 그리기 |

## 확정된 순서

```powershell
movewin.ps1 -Title "Chrome" -X 0 -Y 0 -W 2000 -H 1450   # 두 번 부를 것 (아래 함정 참고)
key.ps1 -Keys "^+b"                                      # 북마크바 숨김 (개인 북마크가 캡처에 들어감)
```

그 다음부터 모든 캡처는:

```powershell
shotbox.ps1 -Out shots\NN.png -X 0 -Y 88 -W 1990 -H 1000 `
            -Front "Chrome" -DismissBar -Wait 12000 `
            -Boxes "x,y,w,h;x,y,w,h" `
            -Hide  "1838,98,62,58;1885,188,78,78"
#                    ↑ 크롬 툴바 프로필 사진   ↑ 구글 콘솔 오른쪽 위 프로필 사진
```

**`-Front` 를 빠뜨리지 마세요.** 사진을 읽는 동안 Claude 창이 앞으로 나와서
크롬 대신 **Claude 화면이 찍힙니다.** v4 에서 실제로 한 장 그렇게 나왔습니다.

**`-Front` 에는 그 탭의 제목 일부를 주세요** (`"Chrome"` 말고 `"데이터 액세스"` 처럼).
`shotbox` 는 모든 최상위 창을 훑어 제목으로 찾습니다. `Get-Process` 의 `MainWindowTitle` 은
프로세스당 하나뿐이라 **창이 둘이면 엉뚱한 창을 앞에 세웁니다.**

> ⚠️ **찍기 전에 「도구가 조종하는 탭이 지금 보이는 탭인지」 확인하세요.**
> 사용자가 다른 탭을 눌러 두면, 브라우저 도구는 배경 탭의 DOM 을 읽고 화면에는 다른 탭이 찍힙니다.
> 좌표는 맞는데 내용이 다른 사진이 나옵니다. `shotbox` 가 찍어 주는 `front:` 줄의 제목이
> 지금 다루는 페이지와 같은지 보면 됩니다. 다르면 탭 띠에서 그 탭을 눌러 앞으로 꺼내세요.

## 좌표는 **고정 오프셋**으로 구한다

이 창 크기에서 페이지 왼쪽 위 모서리는 화면 좌표 **(13, 172)**, 화면 배율 **dpr 2** 입니다.
브라우저에 아래를 넣고 `__r('선택자')` / `__btn('버튼 글자')` 를 부르면 `x,y,w,h` 가 바로 나옵니다.

```js
const OX = 13, OY = 172, D = 2;
window.__S = (r, p = 6) => [Math.round((r.left-p)*D+OX), Math.round((r.top-p)*D+OY),
                            Math.round((r.width+p*2)*D), Math.round((r.height+p*2)*D)].join(',');
window.__r = (s, p) => { const e = document.querySelector(s); return e ? __S(e.getBoundingClientRect(), p) : 'NF:'+s; };
window.__btn = (t, p) => { let h = null;
  for (const e of document.querySelectorAll('button,a[href],summary,[role=button],[role=option],[role=menuitem],nav a')) {
    if (!(e.innerText||'').trim().includes(t)) continue;
    const r = e.getBoundingClientRect(); if (!r.width || !r.height) continue;
    const a = r.width*r.height; if (!h || a < h.a) h = {e, a}; }
  return h ? __S(h.e.getBoundingClientRect(), p) : 'NF:'+t; };
```

> **`outerHeight`·`screenY` 로 오프셋을 계산하지 마세요.** 그 값은 「디버깅 안내 띠」가 떠 있는 동안 측정되고,
> 정작 사진을 찍을 때는 띠가 사라져 있어서 **126px 이 어긋납니다.** v4 에서 이것 때문에 세 번 다시 찍었습니다.

## ⚠️ 반드시 지켜야 붙는 것들

1. **`-DismissBar`** — 크롬 위쪽 「‘Claude’에서 이 브라우저에 대한 디버깅을 시작함」 띠.
   확장이 붙을 때마다 되살아나고 페이지를 126px 밀어 내립니다.
   `(1307,230)` 의 「취소」 알약이 **보라색인지**로 판정하고, `(1925,230)` 의 X 를 눌러 닫습니다.
   **「취소」를 누르면 안 됩니다** — 디버깅이 끊겨 브라우저 도구가 죽습니다.
   - **밝기로 판정하면 안 됩니다.** 대화상자가 뜨면 뒤가 회색으로 어두워져 띠로 오인하고,
     그 X 클릭이 **대화상자를 닫아 버립니다**. 실제로 「프로젝트 선택」 창이 그렇게 닫혔습니다.
     `R-G > 25 이고 B-G > 25`(보라색) 로 보세요
   - **닫는 일은 `-Wait` 가 끝난 뒤에** 해야 합니다. 먼저 닫으면 기다리는 동안 띠가 다시 떠서
     결국 사진에 들어갑니다
   - 닫은 **직후 커서를 화면 밖으로 다시 치우세요.** X 자리가 콘솔 프로필 사진 바로 아래라
     커서를 두면 **계정 이메일 툴팁**이 떠서 찍힙니다
   - **누르기 직전에 한 번 더 확인하세요.** 커서를 옮기는 사이에 띠가 스스로 사라지면
     그 클릭이 **계정 메뉴를 열어** 이름·이메일·사진이 통째로 찍힙니다. 실제로 그랬습니다
2. **`-Wait 12000`** — 확장이 페이지 위에 그리는 **주황 테두리와 가짜 커서**가 사라지는 데 그만큼 걸립니다.
   짧게 주면 캡처에 그대로 들어갑니다. (DOM 에 없어서 자바스크립트로는 못 지웁니다.)
3. **측정한 직후에 바로 찍을 것.** 스크롤이나 늦게 뜨는 요소 때문에 몇 초 만에 좌표가 밀립니다.
   실제로 「Create repository」 네모가 「Public」 위에 그려졌었습니다.
4. **개인정보** — 툴바 프로필 사진(`1838,98,62,58`), 서비스 주소, 클라이언트 **보안 비밀번호**.
   클라이언트 **ID** 는 공개용이라 노출돼도 안전합니다. secret 과 헷갈리지 마세요.

## PowerShell·도구 함정 (v4 에서 실제로 밟은 것)

- **콤마가 뺄셈보다 세게 묶입니다.** `@($a - $x, $b - $y)` 는 `$a - ($x,$b) - ...` 로 해석돼 터집니다.
  → 항목마다 괄호: `@(($a - $x), ($b - $y))`
- **중첩 배열은 `return` 에서 펼쳐집니다.** 사각형 목록은 `[Drawing.Rectangle]` 로 담아 `return ,$list`
- **변수 이름은 대소문자를 안 가립니다.** `$h = 창핸들` 이 `-H` (높이) 파라미터를 덮어써서
  창 높이만 안 바뀌는 일이 있었습니다. 핸들은 `$hwnd` 로 쓰세요
- `movewin` 은 **두 번 불러야** 최대화가 풀리고 크기가 먹습니다
- **`javascript_tool` 은 Promise 를 기다리지 않습니다.** `{}` 만 돌아옵니다.
  → 「누르기」와 「재기」를 **다른 호출로 나누세요**
- 반환값에 **쿼리스트링이 붙은 주소**가 들어가면 도구가 결과를 통째로 막습니다. `location.href` 를 돌려주지 마세요
- 페이지가 새로 뜨면 `window.__r` 같은 도우미가 사라집니다. **이동할 때마다 다시 심으세요**

---

# v4 에서 찍은 것 (`docs/images/setup-NN.png`)

| 번호 | 화면 |
|---|---|
| 01 | 원본 저장소 · `Use this template` 버튼 |
| 02 | 열린 메뉴의 `Create a new repository` |
| 03 | 저장소 이름 `my-googlenote` 입력 · 「is available」 |
| 04 | 공개 범위 `Public` + `Create repository` 버튼 |
| 05 | 사본 저장소가 만들어진 화면 |
| 06 | `Settings` 탭 |
| 07 | 왼쪽 메뉴 `Pages` |
| 08 | `Source` 드롭다운 (`Deploy from a branch`) |
| 09 | 열린 메뉴의 `GitHub Actions` |
| 10 | `Source` 가 `GitHub Actions` 로 바뀐 상태 |
| 11 | 첫 실행이 **빨간 ✗** 로 실패 · `Re-run jobs` |
| 12 | `Re-run all jobs` |
| 13 | 다시 실행해 **초록 ✓** 가 된 목록 |
| 14 | `Your site is live at` (주소는 검게 덮음) |
| 15 | 구글 클라우드 콘솔 상단 **프로젝트 칩** |
| 16 | 「프로젝트 선택」 창의 **새 프로젝트** (프로젝트 목록은 잘라냄) |
| 17 | 인증 플랫폼 **브랜딩** — 앱 이름 · 지원 이메일(가림) |
| 18 | **대상** — 게시 상태(프로덕션) · 사용자 유형(외부) |
| 19 | **데이터 액세스** — `범위 추가 또는 삭제` 버튼 |
| 20 | **클라이언트** — `클라이언트 만들기` 버튼 |
| 21 | **OAuth 클라이언트 ID 만들기** — 애플리케이션 유형 |
| 22 | 「선택한 범위 업데이트」 패널 — `drive`·`userinfo.email`·`openid` 체크된 모습 |
| 23 | 같은 패널 아래쪽 — **직접 범위 추가** 칸과 `테이블에 추가` |
| 24 | 패널 맨 아래 **업데이트** 버튼 |
| 25 | **제한된 범위** 표에 `.../auth/drive` 가 들어간 결과 |
| 26 | **Google Drive API** 페이지 — 여기서는 `관리`·「API 사용 설정됨」 |
| 27 | 애플리케이션 유형 목록의 **웹 애플리케이션** |
| 28 | 유형·이름을 채운 모습 |
| 29 | **승인된 JavaScript 원본**에 주소 넣기 (경로·슬래시 없이) |
| 30 | 맨 아래 **만들기** 버튼 |
| 31 | 클라이언트 목록에서 **클라이언트 ID 복사** |
| 32 | 내 저장소 `index.html` 의 **682행** `OWNER_CLIENT_ID` 와 오른쪽 위 **연필** |
| 33 | 편집 화면의 **Commit changes…** 버튼 |
| 34 | 내 사이트 첫 화면과 **구글 로그인** 버튼 (주소는 덮음) |
| 35 | **계정을 선택하세요** 창 (계정 줄은 덮음) |
| 36 | 로그인 뒤 저절로 열리는 **⚙️ 설정** — 폴더 칸과 `이 폴더로 연결` |

> 34 번부터는 **주소창을 덮었습니다.** 살아 있는 사본 주소가 공개 저장소에 실리면
> 모르는 사람이 들어와 100명 한도를 깎습니다.

> ⚠️ **줄 번호가 바뀌었습니다. `568행` 이 아니라 `682행` 입니다.** 옛 안내서의 숫자는 틀렸습니다.
> 그 위에 「0. 소유자 설정 — 여기만 바꾸면 됩니다」 주석이 있으니 그걸로 찾는 게 안전합니다.

> 26 번은 **이미 켜져 있는 프로젝트**라 `관리` 로 보입니다.
> 처음 만든 프로젝트에서는 같은 자리에 **파란 「사용」 버튼**이 있습니다. 안내서에 그렇게 적으세요.
>
> 27~30 은 **폼만 채우고 「만들기」는 누르지 않았습니다.** 실제 클라이언트를 늘리지 않기 위해서입니다.
> 그래서 「만든 뒤 뜨는 클라이언트 ID 창」은 못 찍었고, 대신 31 번(목록에서 복사)으로 대신합니다.
> **보안 비밀번호가 보이는 화면은 아예 열지 않았습니다.** 안내서에는 「ID 만 복사하고
> 비밀번호는 어디에도 붙여넣지 않는다」고 글로 적으세요.

### 범위 패널을 다룰 때 (setup-22~24)

- 패널 안쪽은 **`.cfc-sub-task-container` 의 `scrollTop`** 으로 굴립니다.
  `scrollIntoView()` 는 이 패널에서 안 먹습니다
- **이 패널에서는 `getBoundingClientRect` 좌표가 150px 쯤 어긋납니다.**
  네모 없이 한 장 찍고 **`boxpng.ps1` 로 이미지 좌표를 보고 그리세요**. 그게 빠릅니다
- **패널의 X 를 좌표로 누르지 마세요.** 스크롤된 뒤라 자리가 달라져 **범위 체크박스를 눌러 버립니다.**
  v4 에서 `logging.admin` 이 그렇게 켜졌습니다(되돌려 놓았고 저장은 안 됨).
  닫을 때는 **페이지를 다시 불러오세요.** 저장 전이면 그걸로 다 사라집니다

> **11~13 은 v4 에서 새로 알아낸 진짜 절차입니다.**
> 템플릿으로 사본을 만들면 Pages 를 켜기 전에 워크플로가 한 번 돌아 **반드시 실패합니다.**
> Pages 를 `GitHub Actions` 로 바꾼 뒤 **`Re-run all jobs` 를 눌러야** 초록이 됩니다.
> 옛 안내서에는 이 단계가 없어서, 따라 하던 사람은 빨간 ✗ 에서 막혔을 것입니다.

---

## v4 에서 실제로 확인한 것 ✅

새로 만든 사본(`legoschool/my-googlenote`)에서 **드라이브를 연결하지 않은 채** 돌려봤습니다.

- 배포된 사본이 정상으로 뜨고 **로드할 때 콘솔 오류가 하나도 없음**
- **구글 로그인이 실제로 성공** — 클라이언트 ID·승인된 JavaScript 원본·범위·프로덕션 게시 상태가
  **새 사본에서 사슬 전체로 맞물린다는 것**이 처음으로 확인됐습니다
- 로그인 뒤 **설정 창이 저절로 열리고** 1단계 완료·2단계 폴더 안내가 나옴
- **기록 작성 → 저장 → 목록에 표시 → localStorage 보관**, 글 속 `[[위키링크]]` 보존
- 블록 버튼 9종(소제목·글·인용·캡처·사진·파일영상·링크·코드삽입·구분선) 모두 있음
- `전체 보기` · `엮어내기(btnCompile)` · `📌 고정` · `🏷 태그 필터` 모두 있음
- **관계망 캔버스가 실제로 그려짐** (가운데 영역 픽셀 확인)

> 점검용으로 만든 기록은 지웠고, **드라이브에는 아무것도 만들지 않았습니다.**

## 드라이브 왕복 — v4 에서 실제로 확인했습니다 ✅

점검용 폴더 `PKEMS 점검용 v4` 를 만들어 연결하고, 사진 한 장 붙인 기록을 저장한 뒤
**드라이브 쪽 파일을 직접 읽어** 대조했습니다.

| 확인 | 결과 |
|---|---|
| **기록별 폴더 생성** | `2026-08-17_경험_연결점검/` 폴더가 생김 ✅ |
| **`.md` 파일** | 그 폴더 안에 `2026-08-17_경험_연결점검.md` (511바이트) ✅ |
| **색인·설정 파일 위치** | `PKEMS-index.json`·`PKEMS-settings.json` 이 **연결 폴더 바로 아래** ✅ ← 가장 중요한 지점 |
| **첨부 업로드** | png 13,912바이트가 같은 폴더에 올라감 ✅ |
| **첨부 경로** | `.md` 안이 `![원래이름](2026-08-17_연결점검_01.png)` 상대경로이고 **같은 폴더에 그 이름 그대로** 있음 ✅ (옵시디언에서도 그대로 보임) |
| **구글 문서 사본** | 같은 폴더에 구글 문서가 함께 생김 ✅ |
| **색인 내용** | `version 3` · `entries[]` 에 blocks·`mdId`·`docId`·`folderId` 가 모두 채워짐 ✅ |

앞머리(front matter)에 `id`·`type`·`title`·`created`·`updated`·`attachments`(원래 이름·mime·driveId)가
모두 들어갑니다. 파일만 보고도 되살릴 수 있는 형태입니다.

### 조각 업로드도 지나갔습니다 ✅

**73.5MB(77,098,711바이트) mp4** 를 붙여 저장했더니 그대로 올라갔습니다.
`RESUMABLE_CHUNK` 가 8MB 이므로 **약 10조각**을 이어 붙인 것입니다.
「업로드 실패: [object ProgressEvent]」의 뿌리였던 `putChunks` 가 **끝까지 돕니다.**

- `.md` 첨부 목록에 원래 이름(`8차시 실습 1번.mp4`)·mime·driveId 가 기록됨
- 본문에는 🎬 드라이브 재생 링크로 걸림 (73MB를 본문에 박지 않는 게 맞습니다)
- **다만 그 안의 `401 재시도` 는 못 봤습니다.** 토큰이 만료되는 **1시간 뒤**라야 지나가는 길입니다

### 폴더 훑어서 가져오기도 확인 ✅

앱이 만들지 않은 `.md` 와 사진을 드라이브에 **직접 넣어 두고** 눌러 봤습니다.

- `# 제목` 을 제목으로, 본문을 텍스트 블록으로 읽어 색인에 넣음
- **같은 폴더의 사진을 찾아 이미지 블록으로 연결**하고, `![대체글]` 의 대체글을 캡션으로 가져옴
- 앱이 만든 기록이 아니므로 `folderId`·`docId` 는 `null` 로 남음 (정상)

## v4 에서 찾아 고친 진짜 결함

### 📚 엮어내기가 눌러도 안 열렸다 (`dfe5bb7`)

사용자가 「엮어내기 눌러도 반응이 없다」고 해서 찾았습니다.

```js
// openCompile() 안, 고를 목록을 그리는 자리
row.appendChild(el("span", "pmeta", (n.createdAt || "").slice(0, 10)));   // ← 여기서 터진다
```

`createdAt` 은 **숫자(epoch ms)** 입니다. 숫자에는 `.slice()` 가 없어서 예외가 나고,
그 예외가 `pool.forEach` 를 뚫고 나가 **`modalHost.appendChild(bg)` 까지 못 갑니다.**
그래서 화면에 아무것도 안 뜨고 **버튼이 죽은 것처럼 보였습니다.**

같은 함수가 이미 쓰고 있던 `localDay()` 로 바꿨습니다.

> 옛 인수인계서의 「`createdAt.slice(0,10)` 은 UTC 라 하루 어긋남」이라는 메모는
> **지금은 더 위험합니다. 하루 어긋나는 게 아니라 그 화면이 통째로 안 열립니다.**
> 날짜를 뽑을 때는 반드시 `localDay()` 나 `formatDate()` 를 쓰세요.

### 엮어낸 `.md` 에서 사진 경로가 깨졌다 (`45d2769`)

엮어내기를 고쳐서 실제로 뽑아 보니 나온 문제입니다. 한 파일 안에서 이렇게 나왔습니다.

```markdown
## 1. 연결점검
![…](2026-08-17_연결점검_01.png)        ← 이 사진은 하위 폴더 안에 있다

## 2. 직접 넣은 노트
![…](점검용_직접넣은노트_01.png)          ← 이 사진은 최상위에 있다
```

**한 파일인데 편마다 기준 폴더가 다릅니다.** 어디에 저장해도 둘 중 하나는 깨집니다.
원인은 엮어내기가 한 편짜리와 **같은 `blocksToMarkdown()`** 을 쓰는데, 그 함수는 사진을
**파일 이름만으로** 가리키기 때문입니다. 한 편짜리는 자료와 같은 폴더에 있으니 맞습니다.

`blocksToMarkdown(n, h, base)` 로 앞에 붙일 폴더를 받게 하고, 엮어내기에서만
`n.folderName + "/"` 을 넘깁니다. **엮은 책은 「연결 폴더 바로 아래」에 두고 보는 것**이 기준입니다.
HTML 로 엮는 쪽은 드라이브 링크를 쓰므로 손댈 것이 없습니다.

**고친 뒤 다시 뽑아 확인했습니다 ✅**

```markdown
1편: ![…](2026-08-17_경험_연결점검/2026-08-17_연결점검_01.png)   ← 폴더가 붙음
2편: ![…](점검용_직접넣은노트_01.png)                            ← 최상위라 그대로
```

목차·앵커·본문·첨부 링크·연결 정보까지 모두 정상이었습니다.

### 「폴더 훑어서 가져오기」를 못 찾겠다 (`dfe5bb7`)

「저장 위치」 탭 **맨 아래 4단계**에 묻혀 있었습니다. 사용자가 「고급」 부터 찾다가 못 봤습니다.
**설정이 아니라 관리 작업**이므로 「고급」 탭 맨 위로 옮기고, 「드라이브에서 기록 다시 불러오기」도
같이 옮겼습니다. 원래 자리에는 길만 한 줄 남겼습니다.

> 이 수정은 **원본과 사본(`my-googlenote`) 양쪽에 넣었습니다.** 사본은 템플릿으로 뜬 별개
> 저장소라 원본을 고쳐도 따라오지 않습니다. 사본에서 확인할 일이 있으면 **같은 수정을 두 번**
> 넣어야 합니다.

## 📚 엮어내기 — 실제로 뽑아 확인 ✅

2편을 `.md` 로 엮어 받아 열어 봤습니다. 목차·앵커(`#ch1-…`)·본문·사진·첨부 링크가 모두 맞았습니다.
경로 결함(아래 참고)을 고친 뒤 다시 받아 폴더가 앞에 붙는 것까지 확인했습니다.


## 크롬 앱으로 설치되게 만들었습니다 (v4, `f89d072`)

`manifest.json` · `sw.js` · `icons/` 를 넣어 **PWA** 로 만들었습니다.
머리말의 **`⬇ 앱으로 설치`** 버튼은 크롬이 설치 가능하다고 알려 줄 때만 나타납니다.

| 파일 | 하는 일 |
|---|---|
| `manifest.json` | 앱 이름·아이콘·독립 창. 경로는 **상대경로**(`./`)라 사본 어디에 올려도 그대로 됩니다 |
| `sw.js` | 설치 조건을 만족시키는 최소한의 서비스 워커 |
| `icons/` | 512·192·180·48·32·16 과 maskable, 그리고 원본(`icon-source.png`) |

> ⚠️ **`sw.js` 는 `index.html` 을 절대 캐시하지 않습니다.**
> 이 앱은 `index.html` 한 개를 고쳐서 배포하는 구조라, 캐시에 물리면
> **고쳐도 옛 화면이 계속 뜹니다.** 오프라인일 때만 안내 문구를 대신 보여 주고,
> 그 외에는 언제나 네트워크로 갑니다. **이 방침을 바꾸지 마세요.**

아이콘을 바꾸려면 512 이상 정사각형 PNG 하나를 준비해 같은 이름들로 다시 뽑으면 됩니다.
`icons/icon-maskable-512.png` 만 **그림을 72% 로 줄여** 가운데 두었습니다(둥글게 잘려도 안 잘리도록).

배포된 두 사이트에서 확인했습니다 — 매니페스트·서비스워커·아이콘 모두 200,
서비스 워커가 페이지를 잡고 있고, 설치 버튼이 실제로 나타납니다.
## 아직 확인 못 한 것

아래 2가지만 남았습니다.

1. **조각 업로드의 `401 재시도`** — 조각으로 쪼개 올리는 것 자체는 73.5MB 로 확인했습니다.
   그 안의 토큰 만료 재시도는 **1시간 뒤**라야 지나가므로 짧은 점검으로는 확인할 수 없습니다
2. **폴더 훑어서 가져오기의 5단계·400곳 상한** — 이번에도 얕은 폴더로만 확인했습니다.
   깊은 폴더로 시험하려면 하위 폴더를 5단계 넘게 만들어 두고 눌러 보세요

> **폴더 훑어서 가져오기의 5단계·400곳 상한**은 이번에도 얕은 폴더로만 확인했습니다.
> 깊은 폴더로 시험하려면 하위 폴더를 5단계 넘게 만들어 두고 눌러 보세요.

> ⚠️ `PKEMS-index.json` 과 `PKEMS-settings.json` 은 **연결 폴더 바로 아래**에 있어야 합니다.
> 이름으로 그 폴더 안에서만 찾기 때문에(`findInFolder`), 하위 폴더로 내리면
> 색인을 못 찾고 빈 것을 새로 만듭니다. 자료 파일은 어디로 옮겨도 파일 ID 로 찾습니다.

## ⚠️ 막힌 것 — 구글 프로젝트 삭제

사용자가 「오래된 3개 지워달라」고 했고, **`unlimit930` 1개만 삭제**했습니다
(2026-09-16 영구 삭제 예정, 그 전까진 복구 가능).
**나머지 `work1` · `CHAGEROOTREPORT1` 는 안전 장치에 막혀 못 했습니다.** 우회하지 않고 멈췄습니다.
**필요하면 사용자가 직접** — 각 프로젝트의 `IAM 및 관리자 → 설정 → 🗑 종료` 에서 프로젝트 ID 입력.

> 표에서 좌표로 클릭하면 오른쪽 정보 패널이 열리며 행이 밀립니다.
> 실제로 엉뚱한 프로젝트(`CHATBOTONLY`, `digitalbase`)가 체크됐었습니다.
> **이름으로 찾아 고르고, 실행 직전에 목록을 다시 읽어 확인하세요.**

---

## 앱 구조 요약 (코드 읽기 전에)

`index.html` 단일 파일. 주석으로 섹션이 번호로 나뉩니다.

```
0    소유자 설정 (OWNER_CLIENT_ID, SCOPE, DEFAULTS, FOLDER_MODES, VIEW_MODES)
1    유틸           2  상태/localStorage/IndexedDB/임시저장
3    구글 인증 (토큰 보관·canOpenPopup·renewQuietly·reconnect)
4    드라이브 폴더 (uploadBlob/putChunk/putChunks·태그폴더·바로가기·공유권한)
5    파일 이름 규칙
5-2  위키링크/백링크/relations
6    마크다운·HTML 생성 (blocksToMarkdown / blocksToHtml / htmlPage)
7    동기화(syncPending, 색인, tombstone, syncTagShortcuts)
8    블록 에디터 (openVoiceModal · openDrawModal · openMaskForBlock · openCropModal)
8-9  임시저장(draft)
9    저장/편집     10  목록 렌더(보기 3종·슬라이드·영상)
10-2 엮어내기 (compileMarkdown / compileHtml / widenByLinks)
11   상단/배너 (updateChrome — «다시 연결» 띠가 여기 있다)
11-8 폴더에서 가져오기 (5단계까지 훑음)
11-9 밖에서 들어온 것 받기 (공유 시트·북마클릿 → intakeShared)
12-0 도움말   12-1 폴더 지도(openFolderMap·squarify) + 관계망   12  설정 모달
13   이벤트   14  시작
```

v5 에서 늘어난 파일: `manifest.json` 에 `share_target`, `sw.js` 에 공유 POST 받기.

### 알아둘 설계 결정

- **첨부는 Blob 으로 들고 IndexedDB 에 보관**. localStorage 에는 절대 넣지 않음
  → 블록 복사는 반드시 `cloneBlock()`. `JSON.parse(JSON.stringify())` 쓰면 **Blob 이 뭉개짐**
- **삭제는 tombstone** 으로 전파
- **관계는 두 종류** — 글 속 `[[링크]]` 와 `relations[]`. `linkData()` 에서 합침
- **코드 삽입은 sandbox iframe**, `allow-same-origin` 절대 주지 말 것
- 캔버스(관계망)는 **매 프레임 크기 동기화** 필요
- **블록→마크다운/HTML 은 함수로 분리돼 있음**(`blocksToMarkdown(n, h)` / `blocksToHtml(n, h)`)
- **날짜만 뽑을 때는 `localDay()`** 를 쓸 것. `createdAt.slice(0,10)` 은 UTC 라 하루 어긋남
- **`slim()` 은 흰 목록(whitelist)** 이다. 기록에 새 항목을 더하면 여기에도 적어야
  다른 기기로 넘어간다 (v5 에서 `shareUrl` 을 더했다)
- **모자이크·손 메모는 «원본 + 덧그린 목록»** 으로 들고 있다가 매번 다시 그린다.
  캔버스에 곧바로 칠하지 말 것 — 되돌리기가 사라진다
- **모달을 닫을 때 치울 것이 있으면 `modalCleanup`** 에 넣는다 (`closeModal()` 이 부른다)
- **공유 창은 `z-index: 170`** 이다. 「전체 보기」가 140 이라 그 위로 올려야 보인다

---

## 로컬에서 띄우는 법

`http://localhost:8000` 이 OAuth 승인된 원본이라 로그인까지 테스트 가능합니다.

```bash
powershell -ExecutionPolicy Bypass -File docs/점검도구/띄우기.ps1
```

저장소를 그대로 `http://localhost:8000` 으로 띄웁니다 (사본을 만들지 않으니 고치면 바로 반영).

> ⚠️ **`.ps1` 파일은 «BOM 붙은 UTF-8» 로 저장해야 합니다.** BOM 이 없으면 파워셸 5.1 이
> ANSI 로 읽어서 경로의 한글이 깨지고 따옴표가 어긋나 파싱 오류가 납니다.
> `[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($true)))`

**이 노트북에 있는 것: git · node(v24.19.0) · python(3.12) · 엣지.** 없는 것: gh CLI.
(예전 인수인계서에 「python·node 없음」이라 적혀 있던 것은 **지금은 틀렸습니다.**)

### 커밋할 때

- 커밋 메시지에 `->` 를 쓰면 PowerShell 이 git 인자로 잘못 넘겨 실패합니다.
  **메시지는 파일로 만들어 `git commit -F 파일`**
- `Out-File -Encoding utf8` 은 **BOM 을 붙여** 커밋 제목 앞에 깨진 글자가 들어갑니다.
  `[System.IO.File]::WriteAllText($p, $t, (New-Object System.Text.UTF8Encoding($false)))` 를 쓰세요
- `D:\구글 기록장\...` 경로에 `Remove-Item` 을 쓰면 보호 규칙에 막힙니다. `git rm` 도 같은 이유로 막힘

---

## 사용자에 대해

- 초등 교사. 이 도구를 **본인이 실제로 쓸 것**이고, 연수·동료 배포도 염두에 둠
- **기술 장벽 없는 것**을 매우 중시함. “일반 사용자에게는 어려워”라는 피드백이 반복됨
- 문서는 **화면에 보이는 그대로** 적어주는 방식을 선호
- 결정이 빠르고, **되묻기보다 만들어서 보여주는 것**을 선호함
- 다만 **계정을 건드리는 일(삭제 등)은 목록을 보여주고 확인받는 편이 좋음** — 실제로 엉뚱한 게 선택돼 있었음
- 밤늦게·연수 중에도 작업함. 화면을 공유 중일 수 있으니 **캡처에 개인정보가 들어가지 않게** 조심할 것
