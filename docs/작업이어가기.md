# 🔖 작업 이어가기

> 이 문서는 **«지금 무엇을 해야 하나»** 만 적습니다. 짧게 유지하세요.
> «어쩌다 이렇게 됐나» 는 [작업기록.md](작업기록.md) 에 있습니다.
> 마지막 정리 **2026-08-23** · `main` 배포 완료 · 점검 **283가지 + 대비 47조합** 전부 통과

---

## ▶ 지금 바로 할 일 · 사람이 눌러 봐야 하는 것

**코드는 다 되어 있습니다.** 다만 **로그인이 필요한 길에는 자동 점검이 한 발도 못 들어갑니다.**
시험용 폴더 하나로 20분이면 됩니다.

| | 무엇 | 볼 것 |
|---|---|---|
| ① | 📥 자료 끌어오기 | **작은 폴더로 먼저.** 원본을 안 건드리니 잘못 들어와도 목록에서 지우면 됩니다 |
| ② | 🌐 웹 쪽으로 공유 | 사진·녹음 든 기록으로 만들어 **시크릿 창에서** 열기. 보이는가 · 재생되는가 · **「공유 끄기」 뒤엔 안 열리는가** |
| ③ | 🔊 틀어 놓고 받아 적기 | 폰 녹음 파일을 불러와 눌러 보기. **이어폰 뺀 상태로.** 얼마나 받아 적히는가 |
| ④ | 색인 조각 | ① 뒤에 폴더에 `TRACE-index-00.json` 이 생기는가. 그 뒤에도 저장이 빠른가 |
| ⑤ | 색감 여섯 | 한 번씩 돌려 보기. 새로 넣은 **「숲」** 과 「공책 + 다른 색」 조합 |
| ⑥ | 폰 | 어둡게 + 아주 크게로 한 번 |

> ⚠️ **되돌릴 수 없는 것은 ②의 링크 퍼짐뿐입니다.** 나머지는 다 되돌아갑니다.

> ②에서 사진이 안 보이고 「드라이브에서 열어 보기」 로 내려가 있으면 **고장이 아닙니다.**
> 사진 주소(`lh3.googleusercontent.com/d/…`)는 구글이 약속한 길이 아니라, 막힐 때를 대비해
> 두 겹을 뒀습니다. 그 줄이 보이면 대비책이 도는 것입니다.

### 📸 SETUP 캡처 넉 장 (하다가 멈춘 자리)

`setup-01`~`04` 에만 옛 저장소 이름(`pkems-googlenote`)이 찍혀 있습니다.
`05`~`40` 은 «읽는 사람 자기 저장소» 화면이라 멀쩡합니다.

**막힌 이유** · 캡처는 바탕화면을 찍는 방식인데, **브라우저 도구로는 탭을 앞으로 못 세웁니다.**
사용자에게 크롬에서 `github.com/legoschool/trace` 탭을 눌러 달라고 부탁해야 합니다.
그다음 `docs/캡처도구/` 의 `movewin.ps1` → `rect.js` → `shotbox.ps1 -Front "legoschool/trace"`.
**`Create repository` 는 절대 누르지 마세요.** 양식만 찍고 나옵니다.

---

## ⛔ 건드리기 전에 반드시 읽을 것

### 1. 이름은 화면이 아니라 «자료» 에도 박혀 있다

`PKEMS` → `PEER` → `TRACE` 로 두 번 바뀌었습니다. 잘못 건드리면 **쓰던 사람의 기록이 통째로 없는 것이 됩니다.**

| 어디 | 지금 | 이어받는 법 |
|---|---|---|
| 드라이브 | `TRACE-index.json` · `TRACE-settings.json` | `findOrInherit()` · 옛 이름을 최근 것부터 찾아 **파일 ID 는 그대로 두고 이름만** 바꾼다 |
| localStorage | `trace.*` | `inheritOldKeys()` · `peer.*` → `pkems.*` 순. **옛 키는 안 지운다** |
| IndexedDB | **`pkems` 그대로** | 아직 못 올린 사진이 들어 있다. 화면에 안 보이는 이름이라 안 바꾼다 |

**저장 키를 건드렸으면 `이름바꾸기.mjs` 를 반드시 돌리세요.** 화면 결함이 아니라 **자료 사고**입니다.

### 2. 색인은 커지면 조각으로 갈린다

256KB 를 넘으면 `TRACE-index.json` 이 «목차» 가 되고 기록은 `TRACE-index-00.json` … 으로 갑니다.

- **조각을 못 읽으면 저장을 멈춥니다** (`indexIncomplete`). 반쪽으로 덮어쓰면 그 조각이 통째로 지워집니다
- **조각을 먼저, 목차를 나중에** 올립니다
- 옛 버전이 목차를 덮어써도 남은 조각을 이름으로 찾아 함께 읽습니다 (`readStrayShards`)

**`saveIndex`·`loadIndex` 를 건드렸으면 `색인쪼개기.mjs` 를 돌리세요.**
「조각을 못 읽으면 한 글자도 덮어쓰지 않는다」 가 빨개지면 **그대로 배포하지 마세요.**

### 3. 보는 것은 축 넷이다. 서로 넘어오면 안 된다

| 축 | 붙는 곳 | 쥐는 것 |
|---|---|---|
| 모양 | `data-theme` | 글꼴 · 모서리 · 테두리 굵기 · 돌기 · 여백. **색 없음** |
| 색감 | `data-tone` | 종이 · 잉크 · 강조 · 유형 여섯. **모양 없음** |
| 밝기 | `data-mode` | (없음)=기기 따름 · light · dark |
| 글자 | `data-size` | (없음) · big · huge |

- **모양 블록에 색을 적지 마세요.** 적고 싶어지면 색감에 적어야 하는 것입니다.
  예외는 «가리키기» 뿐 · `--line: var(--ink)` · `--hard`
- **색감의 어두울 때 값은 두 자리에 놓입니다** (기기 따름 · 사람이 고름).
  손으로 두 벌 쓰지 말고 `docs/점검도구/색감만들기.mjs` 로 찍어 내세요
- 색을 하나라도 손댔으면 **`대비점검.mjs`** 를 돌리세요

### 4. 설명을 붙이지 마세요

쓰기 전에 읽어야 할 글이 있으면 **사람들은 그냥 안 씁니다.**

1. 화면이 이미 말하는 것(단추 이름·라벨·자리글)은 설명하지 않는다
2. 남길 것은 «되돌릴 수 없는 것» 과 «안 되는 것» 뿐
3. 그것도 한 줄로, **그때만**

점검 둘이 이걸 지킵니다 · 「첫 화면에 긴 설명이 없다」 · 「녹음 창에 설명이 없다」.
빨개지면 설명을 또 붙인 것입니다. 정 필요하면 `title` 속성에 넣으세요.

### 5. 긴 줄표는 쓰지 않습니다

저장소 어디에도 없습니다. 문장 끝이면 마침표, 그 밖에는 가운뎃점(·).

---

## 🕳️ 함정 모음 · 그냥 지나가세요

| 함정 | 어떻게 피하나 |
|---|---|
| **띄우기가 죽은 줄 모르고 도구를 의심한다** | 점검이 줄줄이 이상하면 먼저 `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/`. 200 이 아니면 도구 이야기가 아닙니다 |
| **헤드리스 브라우저가 안 죽고 남는다** | 이어 돌리기 전에 포트 9xxx 짜리 msedge 를 죽입니다 |
| **셸이 백틱·백슬래시를 먹는다** | 정규식·경로·백틱이 든 스크립트는 **Write 도구로** 씁니다 |
| **파이썬 쓰기가 중간에 실패하면 파일이 0바이트** | 한글이 든 파일은 **Write/node** 로 씁니다 |
| **`index.html` 은 CRLF** | 여러 줄 치환은 **Edit 도구**로. node/python 의 `\n` 매칭은 안 걸립니다 |
| **`var()` 는 «선언한 곳» 에서 풀린다** | 카드마다 달라지는 값은 카드에서 풀리게. 덮개(`--veil`)를 걷는 방식 |
| **점검 잣대가 배경 «그림» 을 못 본다** | `backgroundImage` 도 함께 봅니다 |
| **이름이 겹친다** | 「블록」을 찾다가 「블록**놀이**」가 잡힙니다. 이름 칸만 정확히 견줍니다 |
| **점검이 엉뚱한 카드를 집는다** | 검색으로 좁힌 뒤에 누릅니다 |
| **브라우저 도구는 탭을 앞으로 못 세운다** | 바탕화면을 찍어야 하면 사용자에게 부탁합니다 |
| **내 화면에 보이는 것 ≠ 파일에 든 것** | 뽑아낸 파일에 모르는 머리줄이 붙어 있었는데 **크롬 확장**이 끼운 것이었습니다 |

---

## 🧪 점검 도구

```bash
powershell -ExecutionPolicy Bypass -File docs/점검도구/띄우기.ps1
```

| 도구 | 가짓수 | 언제 |
|---|---|---|
| `화면점검.mjs` | 108 | 화면을 고칠 때마다 |
| `자료가져오기.mjs` | 65 | 가져오기·공유·가리기를 건드리면 |
| `구석구석.mjs` | 30 | 원래 있던 화면을 건드리면 |
| `소개페이지.mjs` | 27 | `intro.html` 을 고치면 |
| `색인쪼개기.mjs` | 22 | **`saveIndex`·`loadIndex` 를 건드리면 반드시** |
| `공유쪽.mjs` | 19 | `view.html` 을 고치면 |
| `이름바꾸기.mjs` | 11 | **저장 키를 건드리면 반드시** |
| `대비점검.mjs` | 47조합 | **색을 건드리면 반드시** |
| `색감만들기.mjs` | (만드는 도구) | 색감을 고칠 때. 손으로 CSS 를 고치지 말 것 |

자세한 것은 [점검도구/README.md](점검도구/README.md).

---

## 🖼️ 보여 주는 판 (Artifact)

<https://claude.ai/code/artifact/01a03979-06f9-4304-ba28-ad23fd709fbb>

**고칠 때는 새로 만들지 말고 같은 링크에 덮어쓰세요.**
⚠️ 앱을 고치면 이 판도 어긋납니다. **2026-08-23 에 축 넷으로 다시 그렸습니다** ·
판 다섯 · 「축 넷」 · 「모양 일곱」 · 「색감 여섯과 밝기」 · 「보내는 길 넷」 · 「부품」.

---

## 📌 아직 안 한 것

| 무엇 | 왜 미뤘나 |
|---|---|
| `localStorage` 5MB 벽 | 기록이 수천 편이면 넘칩니다. 지금은 안내 문구만 사실대로 고쳤습니다. **정말 넘치는지 보고** IndexedDB 로 옮길 일입니다 |
| 가져온 `.md` 를 고치면 원본이 옮겨지는 것 | 그대로 둘지 제자리에 둘지 **사용자가 정할 일**입니다 |
| SETUP 캡처 넉 장 | 위 참고. 사용자가 탭을 앞에 세워 줘야 합니다 |
